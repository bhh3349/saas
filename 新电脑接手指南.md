# 新电脑接手指南（换机开发）

> 更新：2026-09-07 · 基于 commit `b59ce9d`
> 本指南面向在新电脑上从零接手本项目的开发者。

---

## 1. 拿到代码

**方式 A：git clone（推荐）**

```powershell
git clone https://github.com/bhh3349/saas.git D:\c\saas
```

**方式 B：使用打包文件**

解压 `saas-handover-YYYYMMDD.zip` 到任意目录（路径不要含空格）。

---

## 2. 安装环境

| 工具 | 版本要求 | 用途 |
|------|----------|------|
| Node.js | ≥ 18（建议 22.x LTS） | 三个 Web/后端工程 |
| npm | 随 Node | 依赖安装 |
| HBuilderX | 5.x | 收银 App 编译（必需） |
| JDK | 17 | Android 编译 |
| Android SDK | API 34 | Android 编译 |
| Gradle | 8.11.1 | Android 编译 |
| MuMu 模拟器 | 12（桥接模式） | App 运行调试 |
| adb | Android SDK 自带 | 设备连接 |

### 2.1 Node.js

从 https://nodejs.org 下载 LTS 安装包，安装后验证：

```powershell
node -v   # v22.x
npm -v
```

### 2.2 HBuilderX

从 https://www.dcloud.io/hbuilderx.html 下载「标准版」，安装后在「工具 → 插件安装」里装：

- uni-app (Vue3)
- uts 开发插件

登录 DCloud 账号（需绑手机号），用于 AppID。项目 AppID 已在 `cashier-app/manifest.json`：`__UNI__539B9A9`。

### 2.3 JDK 17 + Android SDK + Gradle

推荐目录结构（与本项目默认配置一致）：

```
D:\dev-tools\
├── HBuilderX\          ← HBuilderX 安装目录
├── jdk-17\             ← JDK 17
├── gradle-8.11.1\      ← Gradle
└── android-sdk\        ← Android SDK
    └── platform-tools\adb.exe
```

设置环境变量：

```
JAVA_HOME = D:\dev-tools\jdk-17
ANDROID_HOME = D:\dev-tools\android-sdk
```

### 2.4 MuMu 模拟器（桥接模式）

1. 安装 MuMu 模拟器 12
2. 设置 → 网络设置 → 开启「桥接模式」，记下模拟器 IP（如 `192.168.1.20`）
3. adb 连接：`adb connect <模拟器IP>:5555`

---

## 3. 安装项目依赖

```powershell
cd D:\c\saas
cd merchant-web; npm install; cd ..
cd output\saas-service; npm install; cd ..
cd output\platform-service; npm install; cd ..
cd output\platform-admin-web; npm install; cd ..
```

> cashier-app 是 uni-app x 工程，不需要 `npm install`。

---

## 4. 配置环境变量

两个后端各复制 `.env.example` 为 `.env`：

```powershell
Copy-Item output\platform-service\.env.example output\platform-service\.env
Copy-Item output\saas-service\.env.example output\saas-service\.env
```

**必改项：**

```ini
# platform-service/.env
SEED_OPERATOR_PASSWORD=你的管理员密码
JWT_SECRET=<32位随机hex>

# saas-service/.env
JWT_SECRET=<32位随机hex>
```

生成随机 hex：`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

---

## 5. 启动服务（按顺序）

```powershell
# 1. 平台端（端口 3100）
cd output\platform-service
npm run start:dev

# 2. 商家端（端口 3200）
cd output\saas-service
npm run start:dev

# 3. 商家后台 Web（端口 5175）
cd merchant-web
npm run dev
```

验证：
- `http://localhost:3100/auth/login` POST `{"username":"admin","password":"<你的密码>"}` → 返回 token
- `http://localhost:5175` 打开商家后台

数据库 `saas.db` / `platform.db` 首次启动自动创建在 `output/*/data/` 下。

---

## 6. 收银 App 运行

### 6.1 HBuilderX 导入项目

1. 打开 HBuilderX → 文件 → 导入 → 从本地目录导入
2. 选择 `cashier-app/` 文件夹

### 6.2 CLI 方式运行（推荐）

```powershell
& "D:\dev-tools\HBuilderX\HBuilderX\cli.exe" project open --path "D:\c\saas\cashier-app"
& "D:\dev-tools\HBuilderX\HBuilderX\cli.exe" devices list
& "D:\dev-tools\HBuilderX\HBuilderX\cli.exe" launch app-android --project "D:\c\saas\cashier-app" --deviceId "<设备序列号>"
```

### 6.3 App 内配置后端地址

App 首页 → 头像 → 环境切换 → 选择「开发环境（桥接模拟器）」。

默认地址已配置为 `http://<本机IP>:3200`（在 `cashier-app/utils/config.uts` 的 `ENVS` 数组里改）。

### 6.4 注册店铺测试

1. 用 admin 登录平台端造激活码：
   ```powershell
   $r = Invoke-RestMethod http://127.0.0.1:3100/auth/login -Method Post -ContentType "application/json" -Body '{"username":"admin","password":"<密码>"}'
   $code = Invoke-RestMethod http://127.0.0.1:3100/admin/codes/batch -Method Post -ContentType "application/json" -Headers @{Authorization="Bearer $($r.data.token)"} -Body '{"count":1,"batch_no":"TEST001"}'
   $code.data.codes[0]   # 12 位激活码
   ```
2. App 登录页 → 去注册 → 填激活码/手机号/密码/店名
3. 注册成功后用新手机号登录

---

## 7. 打印功能测试（WiFi TCP）

1. 热敏打印机连入同一路由（或有 9100 端口的服务）
2. App → 打印 → 添加 → WiFi → 填 IP + 9100 → 保存
3. 如果不知道打印机 IP：App 内有引导（关机 → 长按 FEED 开机 → 小票显示 IP）
4. 打印管理 → 测试打印 → 中文应正常出票（GBK 编码已实现）

无真打印机时，可在电脑上开 TCP 监听验证：

```powershell
$listener = [System.Net.Sockets.TcpListener]::new([System.Net.IPAddress]::Any, 9100)
$listener.Start(); "Listening..."
$client = $listener.AcceptTcpClient()
$stream = $client.GetStream(); $ms = [System.IO.MemoryStream]::new()
$buf = New-Object byte[] 4096
while (($n = $stream.Read($buf, 0, 4096)) -gt 0) { $ms.Write($buf, 0, $n) }
[System.IO.File]::WriteAllBytes("print-output.bin", $ms.ToArray())
"Received $($ms.Length) bytes"
$listener.Stop()
```

---

## 8. 修改后检查

```powershell
# merchant-web
cd merchant-web; npx.cmd tsc -b --pretty false

# saas-service
cd output\saas-service; npm.cmd run typecheck

# platform-service
cd output\platform-service; npm.cmd run typecheck

# 全链路冒烟（需要两个后端运行中）
cd output\saas-service; npm.cmd run smoke
```

---

## 9. 常见问题

| 问题 | 解决 |
|------|------|
| 端口被占 | `Get-NetTCPConnection -LocalPort 3200 -State Listen` 找 PID，`Stop-Process -Id <pid>` |
| HBuilderX CLI 提示「未检测到已打开的 HBuilderX」 | 先启动 HBuilderX 主程序再跑 CLI |
| App 连不上后端 | 检查防火墙是否放行 3200 端口入站 |
| 编译缓存脏 | 删除 `cashier-app/unpackage/cache` 再编译 |
| GBK 打印乱码 | 确认用 GBK 解码（见 `utils/esc-pos.uts` 的 `encodeGbk()`） |
| saas-service 崩溃 | 看终端输出；常见是 `.env` 缺 JWT_SECRET |

---

## 10. 项目文档索引

| 文档 | 内容 |
|------|------|
| `餐饮收银SaaS-PRD.md` | 产品需求权威来源 |
| `开发人员接手指南.md` | 架构/启动/编码规范/踩坑记录 |
| `收银App开发接手指南.md` | App 专项（HBuilderX/编译/打印插件） |
| `美团点餐POS学习笔记.md` | 竞品分析（含打印模块实测） |
| `captures/meituan-print-analysis.md` | 美团打印模块详细分析 |
| `AGENTS.md` | Codex 开发规则（上下文路由） |

