# 平台后台 Web — UI 设计输入文档

| 项 | 内容 |
|---|---|
| 配套文档 | `餐饮收银SaaS-PRD.md`（v0.3，功能以 PRD 为准） |
| 后端 | `output/platform-service`（NestJS，端口 **3100**，CORS 已全开） |
| 本文档作用 | 给 UI 设计师 / 前端开发的**结构与交互约束**：页面清单、每页元素、状态、与后端的对应关系 |
| 风格 | **视觉风格（配色 / 字体 / 圆角 / 阴影 / 组件库选型）由使用方自行确定，本文档不预设、不约束** |

> 本文档只锁「结构层 + 交互层 + 数据层」，不锁「视觉层」。设计师出稿时可按自己喜好发挥视觉，但不得改变页面信息架构、字段、状态与交互流程（否则与后端实现脱节）。

---

## 一、平台后台是什么

平台方运营人员自用的内部管理系统，仅两种功能：**管理激活码**、**管理店铺**。内部使用，桌面端为主（建议适配 1280px 以上宽度，兼容平板）。

整体为「登录页 + 一个带侧边导航的主框架」，无复杂多级页面。

---

## 二、后端接口清单（前端联调依据）

统一响应格式：`{ code: number, message: string, data: T }`，所有请求头带 `Authorization: Bearer <token>`（除登录外）。

| 接口 | 方法 | 用途 | 参数 | 返回 data |
|---|---|---|---|---|
| `/auth/login` | POST | 运营登录 | `{ username, password }` | `{ token, operator: { id, username, role } }` |
| `/admin/codes/batch` | POST | 批量生成激活码 | `{ count(1-1000), batch_no, prefix?（1-8位大写字母/数字，默认 POS） }` | `{ batch_no, count, codes: string[] }` |
| `/admin/codes` | GET | 激活码列表 | `{ batch_no?, status?, page=1, page_size=20 }` | `{ total, items[] }` |
| `/admin/codes/export` | GET | 导出 CSV | 同列表参数；**默认仅导出未使用**，最多 10000 条 | 文件下载（CSV） |
| `/admin/codes/:code/void` | POST | 作废激活码 | 路径参数 code | `{ code, status }` |
| `/admin/shops` | GET | 店铺列表 | `{ keyword?, status?, page=1, page_size=20 }` | `{ total, items[] }` |
| `/admin/shops/:id/status` | POST | 停用 / 启用店铺 | `{ status: 'active' \| 'disabled' }` | 更新后的店铺对象 |

---

## 三、页面清单与导航结构

```
登录（/login）
  └─ 主框架（登录后）
       ├─ 激活码管理（/codes）   ← 默认首页
       └─ 店铺管理（/shops）
```

| 编号 | 页面 | 是否独立页面 | 说明 |
|---|---|---|---|
| P1 | 登录页 | 是 | 未登录一律跳转至此 |
| P2 | 主框架（布局） | 是（壳） | 侧边导航 + 顶栏 + 内容区 |
| P3 | 激活码管理 | 否（挂在 P2 下） | 含批量生成弹窗 |
| P4 | 店铺管理 | 否（挂在 P2 下） | 含停用 / 启用操作 |

---

## 四、页面详设

### P1 登录页

| 项 | 内容 |
|---|---|
| 用途 | 运营账号登录 |
| 元素 | 产品名 / 品牌区（可含 logo 位）；账号输入框；密码输入框（可加显隐切换）；登录按钮（提交中禁用 + loading）；页脚版权信息（可选） |
| 交互 | 提交调 `POST /auth/login`；成功 → 保存 token 跳主框架；失败 → 顶部或表单内提示「账号或密码错误」；输入框校验：两项均必填 |
| 状态 | 提交中（按钮 loading 防重复提交）；失败（保留已填内容，不清空密码可二选一，建议保留） |

> 种子账号（开发环境默认）：`admin / admin123456`，可写在登录页底部的开发提示里（可选）。

### P2 主框架（壳）

| 区域 | 内容 |
|---|---|
| 侧边导航 | 两个菜单项：**激活码管理**、**店铺管理**；当前项高亮；收起态可选（桌面端默认展开） |
| 顶栏 | 左侧可放面包屑或页面标题；右侧展示当前运营账号名 + 「退出登录」（退出 = 清除本地 token → 回登录页） |
| 内容区 | 承载 P3 / P4 页面，建议 1200px 居中或自适应，padding 统一 |

> 前端按路由懒加载两个页面模块；401（token 过期）全局拦截 → 清 token 回登录页。

### P3 激活码管理

**数据来源**：`GET /admin/codes`（列表）、`POST /admin/codes/batch`（生成）、`GET /admin/codes/export`（导出）、`POST /admin/codes/:code/void`（作废）。

**页面结构**（自上而下）：

1. **工具条**：`批量生成`按钮（主按钮）+ 导出按钮（次按钮）；筛选区：批次号输入框（模糊或精确，后端为等值匹配 `batch_no =`，**建议精确输入**）、状态下拉（全部 / 未使用 / 已绑定 / 已作废）、查询按钮、重置按钮。
2. **列表表格**：

| 列 | 字段 | 展示要求 |
|---|---|---|
| 激活码 | `code` | 等宽字体（激活码需人工抄录，建议等宽 + 自动换行/可复制） |
| 批次 | `batch_no` | 文本 |
| 状态 | `status` | 标签：`unused`→未使用（绿）、`used`→已绑定（蓝）、`void`→已作废（灰） |
| 绑定店铺 | `bound_shop_id` | 有值显示店铺（**建议前端关联 `/admin/shops` 显示店铺名**，后端仅返回 id）；为空显示 `—` |
| 绑定时间 | `bound_at` | 空显示 `—`；格式 `YYYY-MM-DD HH:mm` |
| 创建时间 | `created_at` | 同上 |
| 操作 | — | 仅状态为**未使用**的行显示「作废」按钮；其他状态不显示操作 |

3. **分页**：页码 + 每页条数（后端默认 20，上限 100）；底部显示「共 N 条」。

**交互细则**：

- **批量生成弹窗**：字段「数量」（数字，1-1000，默认值如 100）、「批次号」（必填，建议同时给出示例格式说明）、「码前缀」（选填，1-8 位大写字母 / 数字，默认 POS，附示例 `POS-8K3F-2M9Q`）。提交成功 → 关闭弹窗 + 刷新列表 + 成功提示「已生成 N 个激活码」；失败（如数量超限 / 前缀非法）→ 表单内红字提示后端 message。
- **导出**：按当前筛选条件导出 CSV 文件（后端文件名固定 `activation_codes.csv`）；默认导出未使用；导出前可给 loading 提示。
- **作废**：二次确认弹窗，文案建议「确认作废激活码 `POS-XXXX-XXXX`？作废后不可恢复，无法再注册绑定」；确认后调接口 → 成功刷新列表 + 提示；失败（如该码已绑定）→ 提示后端 message（「当前状态（used）不可作废」）。
- **筛选**：筛选条件变化 → 重置页码到 1 重新查询。

**状态要求**：空状态（无数据时显示空态插画 / 文案「暂无激活码」）、加载态（表格 loading）、错误态（查询失败 toast + 保留页面）。

### P4 店铺管理

**数据来源**：`GET /admin/shops`（列表）、`POST /admin/shops/:id/status`（停用 / 启用）。

**页面结构**：

1. **工具条**：搜索框（关键词，后端匹配店铺名 / 地址 / 手机号）；状态下拉（全部 / 正常 / 已停用）；查询 / 重置按钮。
2. **列表表格**：

| 列 | 字段 | 展示要求 |
|---|---|---|
| 店铺 ID | `id` | 数字 |
| 店铺名 | `name` | 文本（建议主列加粗） |
| 地址 | `address` | 文本，超长省略号 + title 提示 |
| 联系方式 | `phone` | 文本 |
| 激活码 | `activation_code` | 等宽字体；空显示 `—` |
| 注册时间 | `created_at` | `YYYY-MM-DD HH:mm` |
| 状态 | `status` | 标签：`active`→正常（绿）、`disabled`→已停用（红/灰） |
| 操作 | — | `active`→显示「停用」；`disabled`→显示「启用」 |

3. **分页**：同 P3。

**交互细则**：

- **停用**：二次确认弹窗，文案建议「确认停用店铺「XX」？停用后该店所有员工**无法登录、无法使用收银业务**（立即生效）。可随时重新启用。」确认 → 调接口 → 刷新列表 + 提示成功。
- **启用**：二次确认弹窗，文案「确认启用店铺「XX」？启用后该店员工可正常登录使用。」（停用是敏感操作，建议启用也保持一致确认流程）。
- 停用 / 启用请求中按钮 loading，防止连点。

**状态要求**：同 P3（空态、加载、错误、分页）。

---

## 五、通用组件与状态约定（设计师需输出的组件）

| 组件 | 约定 |
|---|---|
| 表格 | 支持：行 hover 高亮、列对齐（数字右对齐 / 文本左对齐）、空态插画、loading 覆盖层 |
| 状态标签 | 三态激活码（未使用 / 已绑定 / 已作废）+ 两态店铺（正常 / 已停用），建议色板语义化（成功 / 信息 / 中性 / 危险） |
| 分页器 | 上一页 / 下一页 + 页码 + 总数 + 每页条数选择 |
| 弹窗（Modal） | 用于：批量生成表单、作废 / 停用 / 启用二次确认；要求：遮罩 + 可关闭、确认按钮 loading |
| 表单 | 必填标记、校验红字提示、禁用态、数字输入限制（数量 1-1000、前缀仅大写字母数字） |
| Toast / 提示 | 操作成功 / 失败提示；位置建议统一（右上角或顶部居中） |
| 按钮 | 主按钮（生成 / 查询等）、次按钮（导出 / 重置）、危险按钮（作废 / 停用）三档 |

---

## 六、给设计师的注意事项

1. **内部工具，效率优先**：信息密度可高于对外产品；列表、筛选、批量操作是核心，不要过度做花哨动效。
2. **激活码可读性**：激活码是人工抄录 / 分发的核心对象，必须等宽、可复制、不被截断隐藏。
3. **敏感操作必须有确认**：作废激活码、停用 / 启用店铺，均要求二次确认弹窗，且文案要说明后果。
4. **状态必须可区分**：6 种状态（3 激活码 + 2 店铺 + 账号）用标签 / 色块表达，色觉无障碍（不只靠颜色，建议标签带文字）。
5. **样式风格自主**：配色 / 字体 / 风格 / 组件库（如 Element Plus、Ant Design、Naive UI 等）由使用方自定；建议选一款成熟 Vue3 组件库快速实现，设计师按组件库规范出稿可显著降低实现成本。
6. **交付物建议**：Figma 高保真稿（登录页 + 主框架 + 两列表页 + 批量生成弹窗 + 二次确认弹窗 + 空态 / 加载态 / 错误态各 1）+ 色板 / 字号 / 间距 token。

---

## 七、实现验收清单（前端开发对照）

- [ ] 未登录访问任何业务页 → 重定向登录页；token 失效（401）→ 清 token 回登录页
- [ ] 登录失败提示「账号或密码错误」，成功跳激活码管理
- [ ] 激活码：筛选（批次 / 状态）+ 分页正确；批量生成成功后列表出现新码；导出 CSV 可下载且与筛选一致；仅未使用可作废，作废二次确认
- [ ] 店铺：关键词 / 状态筛选 + 分页；停用 / 启用二次确认；停用后该店登录与业务接口不可用（后端已实现，前端仅展示状态变化）
- [ ] 全部接口按统一响应 `{ code, message, data }` 解析；业务错误以 `message` 提示，不用 HTTP 状态码强判断
