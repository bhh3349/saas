# 商家后台 Web — UI 设计输入文档

| 项 | 内容 |
|---|---|
| 配套文档 | `餐饮收银SaaS-PRD.md`（v0.3，功能以 PRD 为准） |
| 后端 | `output/saas-service`（NestJS，端口 **3200**，CORS 已全开） |
| 本文档作用 | 给 UI 设计师 / 前端开发的**结构与交互约束**：页面清单、每页元素、状态、与后端的对应关系 |
| 风格 | **视觉风格（配色 / 字体 / 圆角 / 阴影 / 组件库选型）由使用方自行确定，本文档不预设、不约束** |
| 版本 | v0.1（仅登录注册页，后续业务页补充） |

> 本文档只锁「结构层 + 交互层 + 数据层」，不锁「视觉层」。设计师出稿时可按自己喜好发挥视觉，但不得改变页面信息架构、字段、状态与交互流程（否则与后端实现脱节）。

---

## 一、商家后台 Web 是什么

商家端（项目二）的**网页管理后台，仅老板使用**（收银员 / 财务使用收银 App，不进 Web）。登录入口同时承担两件事：

1. **登录**：老板手机号 + 密码进入后台全量管理。
2. **注册新店铺**：新商家凭激活码自助开店（与收银 App 的注册入口**并存**，同一后端接口，互不冲突）。

桌面端为主（建议适配 1280px 以上宽度，兼容平板）。

整体为「登录注册页 + 一个带侧边导航的主框架」，无复杂多级页面。本期仅设计**登录注册页**。

---

## 二、后端接口清单（前端联调依据）

统一响应格式：`{ code: number, message: string, data: T }`。除注册 / 登录 / 忘记密码外，请求头带 `Authorization: Bearer <token>`。

| 接口 | 方法 | 用途 | 参数 | 返回 data |
|---|---|---|---|---|
| `/auth/login` | POST | 商家登录 | `{ phone, password }` | `{ token, user: { id, shop_id, phone, name, role, status } }` |
| `/auth/register` | POST | 注册新店铺 | `{ code, phone, password, shop_name, shop_address?, name? }` | `{ message: '注册成功，请登录' }` |
| `/auth/forgot-password` | POST | 忘记密码 | `{ phone }` | `{ message: '如该手机号已注册，请联系客服重置密码' }` |
| `/auth/me` | GET | 当前登录用户（刷新恢复会话） | — | 同 login 的 `user` |

**登录返回 `user` 字段说明**：

| 字段 | 含义 | 前端用途 |
|---|---|---|
| `id` | 用户 ID | 标识 |
| `shop_id` | 店铺 ID | 多租户标识，存本地 |
| `phone` | 手机号 | 展示 |
| `name` | 姓名 | 展示（注册时未填则默认「老板」） |
| `role` | 角色：`boss` / `cashier` / `finance` | **角色拦截依据** |
| `status` | 账号状态：`active` / `disabled` | 停用提示 |

---

## 三、页面清单与导航结构

```
登录注册页（/login）
  └─ 主框架（登录后，B3 阶段补设计）
       ├─ 菜品管理
       ├─ 桌台管理
       ├─ 结账方式管理
       ├─ 看账
       ├─ 账号管理
       └─ 店铺设置
```

| 编号 | 页面 | 是否独立页面 | 说明 |
|---|---|---|---|
| P1 | 登录注册页 | 是 | 未登录一律跳转至此；内含「登录 / 注册新店铺」双 tab |

---

## 四、页面详设

### P1 登录注册页

**单页 + 双 tab**（不跳独立页面、不弹窗）。默认展示「登录」tab，点击「注册新店铺」切换为注册表单。

**整体布局**：单屏居中卡片。

```
┌──────────────────────────────────────┐
│  [Logo 位] 餐饮收银 SaaS · 商家管理后台 │
│  ┌──────────┐ ┌──────────────┐       │
│  │  登  录  │ │ 注册新店铺   │  ← tab │
│  └──────────┘ └──────────────┘       │
│   （以下内容随 tab 切换）              │
│  ┌────────────────────────────────┐  │
│  │   [登录表单] / [注册表单]        │  │
│  │   错误提示条 / 按钮 loading       │  │
│  └────────────────────────────────┘  │
│  （页脚版权信息可选）                 │
└──────────────────────────────────────┘
```

#### Tab A：登录

| 项 | 内容 |
|---|---|
| 字段 | 手机号（11 位数字）；密码（可显隐切换） |
| 辅助 | 「忘记密码？」链接（置于密码框下方或表单角落） |
| 主按钮 | 「登录」，提交中禁用 + loading |
| 校验 | 手机号：11 位数字格式；密码：必填；前端校验失败就地红字提示，不弹 alert |
| 交互 | 调 `POST /auth/login`；成功后存 token + user，按角色分流：`boss` → 进入主框架；`cashier` / `finance` → 提示「该账号为收银员 / 财务，无后台管理权限，请使用收银 App」，**清除已存 token，停留本页** |
| 失败提示 | 「账号或密码错误」；「账号已被停用」；「店铺已停用，请联系平台」 |
| 状态 | 提交中（按钮 loading 防重复提交）；失败（保留已填内容不清空） |

#### Tab B：注册新店铺

| 项 | 内容 |
|---|---|
| 字段 | 激活码 *、店铺名称 *、店铺地址（选填）、手机号 *、老板姓名（选填）、登录密码 *、确认密码 * |
| 主按钮 | 「注册」，提交中禁用 + loading |
| 前端校验 | 激活码：12 位大小写字母 + 数字，无分隔符（如 `aB7xK2mQ9pLw`）；手机号：11 位；密码：≥ 6 位；确认密码：与密码一致 |
| 交互 | 调 `POST /auth/register`；成功 → 提示「注册成功」→ **自动切回登录 tab 并预填手机号**，用户输密码即可登录 |
| 失败提示 | 激活码无效 / 已使用 → 「激活码格式不正确」或后端 message；手机号已注册 → 「该手机号已注册」 |
| 状态 | 提交中（按钮 loading）；失败（保留已填内容） |

**字段顺序（注册表单，自上而下）**：激活码 → 店铺名称 → 店铺地址（选填）→ 手机号 → 老板姓名（选填）→ 登录密码 → 确认密码。激活码与手机号建议在输入框右侧或下方显示格式提示（如「12 位字母 + 数字」）。

**交互细则**：

- **忘记密码**：点击「忘记密码？」→ 就地展开或小弹窗输入手机号 → 调 `POST /auth/forgot-password` → 统一提示「如该手机号已注册，请联系客服重置密码」（本期不做短信，**不暴露账号是否存在**）。
- **注册成功 / 失败**均停留在本页，不跳转；成功仅自动切 tab + 预填手机号。
- 回车提交：登录 / 注册表单内回车均触发当前 tab 的主按钮。

**状态要求**：提交中（按钮 loading）；失败（表单内红字提示，不弹原生 alert）。

---

## 五、通用组件与状态约定（设计师需输出的组件）

| 组件 | 约定 |
|---|---|
| Tab | 双 tab 切换（登录 / 注册新店铺），当前项高亮，切换仅改表单内容，不跳页 |
| 输入框 | 手机号限制数字输入；密码带显隐切换；激活码等宽字体（需人工抄录） |
| 主按钮 | 「登录」「注册」，提交中 loading 态 |
| 错误提示条 | 表单顶部或字段下方红字，展示后端 `message`；成功提示可用 Toast |
| 表单校验 | 必填标记、格式校验红字、两次密码一致性 |

---

## 六、给设计师的注意事项

1. **双入口并存**：登录页同时承担登录 + 注册，两个 tab 权重需平衡，避免注册抢登录的视觉焦点（登录仍为默认动作）。
2. **激活码可读性**：激活码 12 位字母数字混合（无分隔符），需等宽、可复制，可带格式提示文案。
3. **角色拦截是硬约束**：界面需为「无后台权限」场景预留提示位（收银员 / 财务登录后必须拒绝进入后台）。
4. **不锁视觉**：配色 / 字体 / 组件库自由发挥。**建议**参考平台后台已落地的视觉 token（主色 `#5E6AD2`、深色面板 `#0F1012` / `#141517`、边框 `#242428`、圆角 8px），两后台同属一套产品，视觉统一可降低整体开发成本。
5. **交付物建议**：Figma 高保真稿（登录 tab / 注册 tab / 忘记密码态 / 校验错误态 / 按钮 loading 态 / 无后台权限提示态）+ 色板 / 字号 / 间距 token。

---

## 七、实现验收清单（前端开发对照）

- [ ] 未登录访问任何业务页 → 重定向本页；token 失效（401）→ 清 token 回本页
- [ ] 登录：手机号 + 密码 → 成功进入主框架；`boss` 正常进入，`cashier` / `finance` 提示无权限并清 token
- [ ] 登录失败文案：账号或密码错误 / 账号已停用 / 店铺已停用，均表单内红字提示
- [ ] 忘记密码：输入手机号 → 统一提示「联系客服重置」，不暴露账号是否存在
- [ ] 注册：7 个字段（激活码 / 店铺名 / 地址 / 手机号 / 姓名 / 密码 / 确认密码）前端校验齐全；提交成功自动切回登录并预填手机号
- [ ] 注册失败：激活码无效 / 已使用、手机号已注册，均展示后端 `message`
- [ ] 全部接口按统一响应 `{ code, message, data }` 解析；业务错误以 `message` 提示
