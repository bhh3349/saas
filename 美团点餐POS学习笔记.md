# 美团点餐 POS 学习笔记

> 来源：MuMu 模拟器 `美团点餐助手 V6.27.1000`，包名 `com.sankuai.erp.ng.waiter`。  
> 截图在 `captures/51~65_meituan_pos_*.png`，登录页截图已删除，避免保留账号/密码信息。

## 1. 信息架构

首页不是传统侧边栏，而是全屏「能力入口」网格：

- 桌台
- 点餐
- 接单
- 沽清
- 会员
- 优惠券无单核销
- 设置

顶部保留商户名和商户号，方便多店/多账号场景确认当前上下文。

## 2. 桌台页

桌台页的核心是「高密度状态卡」：

- 顶部搜索：桌台名称 / 首字母 / 助记码。
- 区域切换：大厅、外摆。
- 按区域分组渲染，每行三列卡片。
- 空闲桌台：白底，只显示桌台名和 `0/4` 这类座位数。
- 使用中：橙色底，显示 `4/4`、用餐时长、当前金额。
- 超时/已结清未清台：红色底，金额继续显示，并叠加「已结清」状态章。
- 右上角「联台」入口，带扫码图标。

点空闲桌台后不是先跳菜单，而是弹底部数字键盘：

- 标题带桌台名，例如「请输入就餐人数 (厅7)」。
- 大号数字实时显示当前人数。
- 同一屏可继续设置：会员登录、桌台备注、选择提成人、添加儿童。
- 右下角「开台」是主操作。

## 3. 订单详情

使用中订单详情的信息层级：

- 顶部：桌台名 + 用餐人数，人数可编辑。
- 右上：会员、操作。
- 快捷动作：菜品预览、打印预结单。
- 整单备注：直接放在桌台信息下方。
- 菜品区：菜名、必选标记、数量、金额、下单时间、创建人、已下单时长。
- 底部常驻双按钮：`加菜` / `去结账`。

右上角「操作」弹出一组订单级操作：

催菜、起菜、转菜、退菜、划菜、赠菜、订单打折、订单减免、菜品备注、整单操作、拼桌、转台、并台、撤单、补打客单、补打制作单。

这个面板把「低频但重要」的操作集中在一个入口，避免挤占主流程。

## 4. 点菜页

点菜页要点：

- 顶部搜索支持菜名或数字助记码。
- 左侧分类栏，右侧菜品列表。
- 无规格菜直接显示 `+`；有规格/做法的菜显示「选择」。
- 底部常驻购物车条：合计金额 + 已点菜品数 + 去购物车。
- 首次进入会询问偏好：优先按菜品分类点菜，还是优先按搜索菜品点菜。

点击菜品后的数量面板：

- 展示菜名、单价、备注入口。
- 数量用大号 stepper。
- 主按钮直接显示合计金额，例如「¥28 选好了」。

## 5. 沽清管理

沽清页和点菜页共用同一套「分类栏 + 菜品列表」结构，降低学习成本：

- 支持搜索菜名 / 数字助记码。
- 顶部显示已设置沽清品项数量。
- 底部提供「批量取消沽清」「批量设置沽清」。

## 6. 可借鉴到收银 App 的点

### 优先级较高

| 美团做法 | 对我们 App 的启发 |
|---|---|
| 桌台卡显示人数、时长、金额、状态 | 桌台页继续强化单卡信息密度，减少进入详情才能判断状态 |
| 桌台搜索支持首字母/助记码 | 可给桌台增加拼音/助记码搜索，尤其适合多桌门店 |
| 空闲桌台一键开台 + 数字键盘 | 我们已有开台弹窗，可把人数键盘做成底部大键盘，提高收银机可点性 |
| 订单详情底部固定 `加菜 / 去结账` | 我们已有类似动作，可保持主操作稳定，不随页面滚动 |
| 订单操作集中面板 | 可把退菜、撤单、补打、折扣、备注等低频操作聚合到「更多操作」 |
| 点菜页数字助记码 | 后端/菜品模型可增加助记码字段，收银员盲打效率高 |

### 后续增强

| 美团做法 | 说明 |
|---|---|
| 桌台状态色彩语义 | 我们可定义：白=空闲、橙=进行中、红=超时/待清台，避免照抄美团的具体颜色 |
| 超时时长显性化 | 当前订单可显示「已下单 xx 分钟」，超时用颜色提醒 |
| 沽清批量操作 | 菜品沽清目前更适合后台管理，POS 端后续可做批量快捷沽清 |
| 开台时可录会员/提成/儿童数 | 依赖业务模型扩展，不适合第一版强行塞进开台流程 |
| 拼桌 / 并台 / 转台 | 涉及订单、桌台和状态机调整，应单独设计后端契约 |

## 7. 设计原则

美团 POS 的可用性主要来自四点：

1. **单屏高信息密度**：桌台、金额、人数、时长、状态一次看全。
2. **主流程稳定**：开台、加菜、结账始终是大按钮，不被低频操作干扰。
3. **低频操作集中**：订单操作、打印、退菜、转台等收进二级面板。
4. **键盘优先**：POS 场景大量使用大号数字键盘和大触控区，减少依赖小控件。

我们后续改造收银 App 时，应该优先吸收交互密度和操作分层，不直接照抄视觉风格和业务流程。

## 8. 外9 全流程实测

实测设备：MuMu 模拟器 `192.168.1.20:5555`，应用版本 `V6.27.1000`。截图在 `captures/state-95.png` 到 `captures/state-133.png`。

### 8.1 下单和加菜

第一轮点单如下：

| 分类 | 菜品 | 数量 | 金额 |
|---|---|---:|---:|
| 锅底 | 红汤锅底 / 中辣 | 1 | ¥25 |
| 素菜 | 冬瓜、井水豆芽、黑豆腐、生菜、老南瓜 | 各 1 | ¥24 |
| 荤菜 | 精品毛肚、大刀腰片、老肉片、贡菜丸子、自发鱿鱼 | 各 1 | ¥129 |
| 酒水 | 国宾 | 10 | ¥80 |
| 酒水 | 大窑 | 3 | ¥24 |
| 小吃 | 蛋炒饭 / 备注加点老干妈 | 1 | ¥10 |

这一轮合计 `¥292 / 25份`。应用没有按开台人数自动添加餐位费，需要手动在「锅底油碟」分类添加 `餐位费 ¥6/份 ×4`，补齐后合计变为 `¥316 / 29份`。点击「立即下单」后出现「下单成功」，桌台卡显示 `4/6`、用餐时长和 `¥316`。

第二轮加菜：`生扣鸭肠 ¥16/份 ×1`、`干贡菜 ¥12/份 ×1`，合计变为 `¥344 / 31份`。蛋炒饭的备注能在规格弹窗内录入，并随订单明细显示为「备注：加点老干妈」。

### 8.2 退菜

退菜入口在订单详情右上角「操作 → 退菜」。应用先按菜品勾选，再进入数量和原因确认弹窗：

| 菜品 | 退菜数量 | 原因 | 金额影响 |
|---|---:|---|---:|
| 干贡菜 | 1 | 多点 | -¥12 |
| 国宾 | 4 | 多点 | -¥32 |

退菜后，国宾剩 `x6 / ¥48`，账单价格合计变为 `¥300`。订单明细会用「退」标记分别显示已退数量、原因和时间。

### 8.3 结账

结账页选择「菜品8折」后，账单明细显示：

```text
菜品价格合计  ¥300
菜品8折       -¥60
应收          ¥240
```

`¥240` 本身已是整数，「抹零 → 抹角」无可抹金额，界面上「抹分」「抹角」呈禁用状态，最终金额保持 `¥240`。

混合支付按顺序录入口：

1. 选择「现金-人民币」，把默认金额改为 `¥100`。
2. 再选择「支付宝（自）」，应用默认填充剩余金额 `¥140`。
3. 底部变为「付款完成，确认结账」，确认后外9回到 `0/6` 空闲状态。

需要特别注意：现金金额弹窗默认填入全部应收；用数字键盘删除时，删除一位后界面会实时显示「应收 ¥240，还差 ¥216」，所以应先删到空值再输入新金额，避免把默认金额叠加或误填成超收金额。

### 8.4 对自家收银 App 的验证点

- 餐位费是否应按人数自动建议，但允许收银员手动取消。
- 规格弹窗中的做法/备注要随订单行保存，并在订单详情可见。
- 退菜应支持按菜品选择、按数量部分退、强制选择原因，并留下审计信息。
- 菜品折扣要区分「菜品价格合计」与整单优惠，避免把餐位费或酒水纳入错误折扣范围。
- 混合支付要支持先付部分金额、自动计算剩余金额，并在最后一笔支付时预填余款。
- 支付完成后应立即释放桌台，避免出现「已结清未清台」的额外手动步骤。

### 8.5 快速收银设置页

这一页说明「此设置仅在快餐点餐时生效」，核心能力是选择快捷支付区要展示的支付方式：

- 页面包含「现金 / 会员卡 / 前台码 / 支付宝（自）/ 微信（自）」等开关。
- 开关状态会同步到上方预览区：开启支付宝后，预览区从「现金 + 更多支付」变为「现金 + 支付宝（自） + 更多支付」。
- 关闭支付宝后，预览区恢复为「现金 + 更多支付」。
- 这类设置的触控热点很小，实测支付宝开关实际区域是 `[885,1558][1035,1648]`；点击区域略偏下会没有反应，自家 App 应给开关保留更大的可点击热区。

## 9. 桌面端打印模块实测

详见 `captures/meituan-print-analysis.md`。桌面端 `美团收银智能版.exe` 的打印模块
比移动端点餐助手更完整，共 5 个页面：打印任务 / 打印机管理 / 档口管理 / 票据样式设置 / 菜品打印配置。

核心架构：**档口 → 打印机 → 票据类型** 路由模型，档口绑定打印机后自动路由全部关联票据。
票据样式支持预置模板多选 + 三栏可视化编辑器（字段级字号/对齐/加粗/红黑/分割线）。

对收银 App 的直接可落地项（按优先级）：

1. **打印机 IP 获取引导**（照抄网口向导第 3 步文案：关机→长按 feed 开机→小票显示 IP）
2. **打印页码开关**（多页票据防漏打）
3. **菜品未配置档口警告条**（配置页顶部 + 一键跳转）
4. **测试单按钮前置**（每张票据样式直接试打，不走小票队列）
5. **打印任务历史**（按打印机/票据类型/订单号筛选）
