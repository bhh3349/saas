# 餐饮收银 SaaS — 产品需求文档（PRD）

| 项 | 内容 |
|---|---|
| 产品名称 | 餐饮收银 SaaS |
| 版本 | v0.3（老板 App 后台管理版） |
| 日期 | 2026-08-21 |
| 状态 | 待评审（已纳入三项目结构与架构决策） |
| 定位 | 面向手机端 / 网页端的餐饮收银系统，一套云端服务服务多家餐厅；平台方自研激活码体系 |

**v0.3 变更说明**

1. 老板 App 精简后台升级为**后台管理**：新增菜品管理、桌台管理、结账方式管理；「看当日账」扩展为当日营业概览 + 按日期 / 时间范围查询营业数据（v0.2 仅改价 / 沽清 / 看当日账）

**v0.2 变更说明（历史）**

1. 明确产品为**三个独立项目**：平台方网页后台（运营端）/ 商家 SaaS（网页后台 + 收银 App）/ 顾客扫码点餐（二期）
2. 激活码系统由**平台端自研**（v0.1 误标为「平台方现有能力」），补运营侧生成 / 管理需求
3. 后端由单服务改为**双服务架构**（platform-service / saas-service），避免多进程并发写单 SQLite 的锁冲突
4. 员工 App 权限确认为**完整收银、无后台**；商家后台确认为**网页端**（v0.1 误写成 App 内后台）
5. 顾客扫码点餐明确**二期**，本期仅预留接口设计

---

## 一、产品概述

餐饮收银 SaaS 是一套部署在云端的餐厅收银系统。产品由三个项目构成，共享一套多租户理念，按店铺严格隔离数据：

### 1.1 三项目结构

| 项目 | 端形态 | 使用方 | 核心模块 |
|---|---|---|---|
| **项目一：平台方网页后台** | Web | 公司运营人员（自用） | 激活码管理、店铺信息管理 |
| **项目二：商家 SaaS** | 网页后台 + 手机 App | 餐厅商家 | 商家后台 Web（全量管理）；收银 App（员工 / 老板） |
| **项目三：顾客扫码点餐** | 小程序 / H5（二期） | 顾客 | 扫码点餐、下单、加菜催菜 |

### 1.2 产品形态

```
项目一：平台方网页后台（运营端，自用）
  激活码生成 / 管理 · 店铺信息管理
        │ 内部接口（激活码 claim / 店铺主数据）
        ▼
项目二：商家 SaaS（对外销售）
  ├─ 商家后台 Web（全量管理端，仅老板）
  └─ 收银 App（uni-app x）
       ├─ 员工 App：完整收银（开台 / 点餐 / 接单 / 结账），无后台
       └─ 老板 App：完整收银 + 后台管理（菜品 / 桌台 / 结账方式 / 营业数据）
        │
        ▼
项目三：顾客扫码点餐（二期，本期预留接口）
```

### 1.3 核心原则

- **手机优先**：收银端所有操作面向触屏重做，交互简单直接
- **收银为核**：开台 / 点餐 / 接单 / 结账记账一条线走通
- **记账不收款**：结账 = 记账，不接支付通道
- **数据双保险**：云端主源 + 老板手机本地备份

---

## 二、产品目标与成功指标

### 2.1 产品目标

1. 商家能通过收银 App 完成「开台 → 点餐 → 接单 → 结账记账」的完整收银闭环
2. 老板通过商家后台 Web 与收银 App 后台管理维护店铺，App 内可完成菜品 / 桌台 / 结账方式管理与营业数据查看
3. 新商家凭激活码自助注册，1 分钟内完成开店
4. 运营人员可在平台后台批量生成 / 管理激活码，查看全量店铺信息
5. 数据云端 + 本地双备份，任一方丢失可恢复

### 2.2 成功指标

| 指标 | 目标 |
|---|---|
| 收银闭环完整度 | 收银员可独立完成一天完整营业（开台→点餐→结账→交班） |
| 注册闭环 | 新商家凭激活码 1 分钟内自助注册开店并登录 |
| 激活码运营 | 运营可批量生成、按批次 / 状态查询、作废激活码 |
| 数据可靠性 | 每日备份成功率 100%，可从本地随时恢复 |
| 多租户隔离 | 任何接口不得跨店访问（按租户强制隔离） |

---

## 三、目标用户与角色权限

### 3.1 目标用户

| 用户 | 使用场景 |
|---|---|
| 平台运营 | 在平台后台批量生成激活码、查看全量店铺、处理作废 / 停用 |
| 餐厅老板 | 注册开店、全量管理店铺（网页后台 + App 后台管理）、日常收银（App） |
| 收银员（员工） | 开台、代客点餐、接单、结账记账（仅 App） |
| 财务 | 查看当日与历史营业额、订单明细（仅看账） |

### 3.2 角色权限（商家端固定三种，不开放自定义）

| 角色 | 权限范围 | 说明 |
|---|---|---|
| **老板** | 收银工作台 + App 后台管理（菜品 / 桌台 / 结账方式 / 营业数据）+ 商家后台 Web 全量管理 | 全功能 |
| **收银员** | 仅收银工作台（开台 / 点餐 / 接单 / 结账） | 不可进入任何后台管理 |
| **财务** | 仅看账 | 只能看账目与报表，不可操作 |

> 权限由后端在登录时下发，App / Web 按角色渲染入口。

---

## 四、产品范围

### 4.1 本期范围

| 端 | 范围 |
|---|---|
| **平台方网页后台（项目一）** | 激活码管理（批量生成 / 查询 / 作废）、店铺信息管理（全量列表 / 状态管理） |
| **商家后台 Web（项目二）** | 菜品 / 桌台 / 结账方式 / 看账 / 账号管理 / 店铺设置，仅老板登录 |
| **商家收银 App（项目二）** | 登录注册、收银工作台（今日概览 / 桌台叫号 / 点餐 / 接单 / 结账）；老板 App 另含后台管理（菜品 / 桌台 / 结账方式 / 营业数据） |
| **云端后端（项目一 + 二）** | 平台端服务（激活码 / 店铺主数据）+ 商家端服务（账号 / 业务数据 / 多租户隔离） |

### 4.2 暂缓范围

- 顾客扫码点餐（项目三，二期；本期仅预留接口设计）
- 支付通道接入
- 打印硬件对接（蓝牙 / USB / 网口）
- 老板手机本地备份机制

---

## 五、功能需求

### 5.1 平台方网页后台（项目一，运营端）

#### 5.1.1 激活码管理

| 功能点 | 需求说明 | 验收标准 |
|---|---|---|
| 批量生成 | 输入数量 / 批次号，生成随机激活码 | 码格式唯一，12 位大小写字母 + 数字，无分隔符（如 `aB7xK2mQ9pLw`） |
| 列表查询 | 按批次 / 状态（未使用 / 已绑定 / 已作废）筛选 | 显示码、批次、状态、绑定店铺、绑定时间 |
| 作废 | 对未使用的码作废处理 | 作废后不可再注册绑定 |
| 导出 | 导出未使用激活码 CSV 给销售 | 可直接分发 |

#### 5.1.2 店铺信息管理

| 功能点 | 需求说明 | 验收标准 |
|---|---|---|
| 店铺列表 | 全量店铺（跨租户视角） | 显示店铺名 / 地址 / 激活码 / 注册时间 / 状态 |
| 店铺状态 | 停用 / 启用店铺 | 停用后该店登录与业务接口不可用 |

### 5.2 商家后台 Web（项目二，仅老板）

| 模块 | 功能点 |
|---|---|
| **菜品管理** | 菜品增删改、上下架、价格、规格、分类管理、沽清（售罄置灰） |
| **桌台管理** | 桌台增删改、区域 / 分区；点餐模式切换（桌台 / 叫号） |
| **结账方式管理** | 结账方式增删改（现金 / 微信 / 支付宝 / 赊账 / 免单等） |
| **看账** | 当日 / 历史营业额、订单明细、结账方式汇总（财务角色仅此入口） |
| **账号管理** | 员工账号增删改；角色仅可设：老板 / 收银员 / 财务 |
| **店铺设置** | 点餐模式（桌台 / 叫号）、接单模式（直接下单 / 接单确认） |

### 5.3 商家收银 App（项目二，员工 / 老板）

#### 5.3.1 登录与注册

| 功能点 | 需求说明 | 验收标准 |
|---|---|---|
| 登录页 | App 打开即登录页 | 账号 / 密码输入、登录按钮、忘记密码入口、右上角「注册新店铺」 |
| 登录 | 调 `POST /auth/login` | 成功后进入收银工作台（按角色显示入口）；失败提示「账号或密码错误」 |
| 忘记密码 | 调 `POST /auth/forgot-password` | 输入手机号，提示「如该手机号已注册，请联系客服重置」（本期不做短信） |
| 注册新店铺 | 右上角入口 → 注册页 | 字段：激活码 / 手机号 / 登录密码 / 店铺名称 / 店铺地址；提交调 `POST /auth/register` |
| 激活码校验 | 注册时实时校验 | 无效 / 已用激活码拒绝注册并提示 |
| 手机号唯一 | 注册时校验 | 已注册手机号提示「该手机号已注册」 |
| 注册成功 | 平台端建店铺 + 绑定激活码，商家端建老板账号 | 返回成功，可立即登录 |

#### 5.3.2 收银工作台（老板 / 收银员均可用）

| 功能点 | 需求说明 |
|---|---|
| **今日概览** | 顶部常驻：今日订单数、营业额、各结账方式汇总 |
| **桌台 / 叫号列表** | 桌台模式：桌台网格（空闲 / 占用 / 就餐中）；叫号模式：取餐队列（待制作 / 待取餐 / 已完成） |
| **点餐下单** | 选桌 / 取餐号 → 菜单点餐（选规格、数量）→ 下单；支持商家代客点餐 |
| **接单 / 看单** | 实时订单流；接单确认模式下可接单 / 拒单，直接下单模式进单即生效 |
| **结账记账** | 选择结账方式（后台可配置）、记录实收金额与找零；支持已结账 / 免单 / 挂账 |
| 桌台状态流转 | 仅允许 空闲 → 占用 → 空闲（结账释放） |

> 员工 App 与老板 App 在收银工作台功能一致；差异仅在后端管理入口。

#### 5.3.3 老板 App 后台管理（仅老板）

| 功能点 | 说明 |
|---|---|
| **菜品管理** | 菜品增删改、上下架、价格、规格、分类管理、沽清（售罄置灰 / 恢复） |
| **桌台管理** | 桌台增删改、区域 / 分区；点餐模式切换（桌台 / 叫号） |
| **结账方式管理** | 结账方式增删改（现金 / 微信 / 支付宝 / 赊账 / 免单等） |
| **当日营业概览** | 当日营业额、订单数、各结账方式汇总 |
| **营业数据查询** | 选择日期 / 时间范围查看营业数据（营业额、订单明细、结账方式汇总） |

> 老板 App 后台管理覆盖日常高频管理操作，接口与商家后台 Web 共用，仅入口不同；Web 仍为全量管理端。

### 5.4 云端后端（双服务）

#### 5.4.1 平台端服务 platform-service（项目一）

| 模块 | 需求 |
|---|---|
| 激活码实体 | `code`（唯一）/ `batch_no` / `status`（unused / used / void）/ `bound_shop_id` / `bound_at` / `created_at` / `expired_at` |
| 激活码状态机 | unused → used（注册绑定）；unused → void（作废）；used / void 不可再变更 |
| 批量生成 | `POST /admin/codes/batch`：生成 N 个码，写入批次 |
| 列表查询 | `GET /admin/codes`：按批次 / 状态筛选 |
| 作废 | `POST /admin/codes/{code}/void` |
| 店铺主数据 | 店铺实体（店铺名 / 地址 / 联系方式 / 激活码 / 状态），shopId 源头 |
| 内部接口 | `POST /internal/activation/claim {code, shopName, phone}` → 校验激活码 → 建店铺 → 绑定 → 返回 `{success, shopId}`；内网签名鉴权 |

#### 5.4.2 商家端服务 saas-service（项目二）

| 模块 | 需求 |
|---|---|
| 注册 | `POST /auth/register`：手机号唯一 → 调平台端 `/internal/activation/claim` → 用返回 shopId 建老板账号 + 店铺初始配置（默认桌台 / 默认结账方式） |
| 登录 | `POST /auth/login`：账号密码 → 签发 JWT（含 userId / shopId / role）→ 下发用户信息 |
| 忘记密码 | `POST /auth/forgot-password`：统一返回联系客服提示，不暴露账号是否存在 |
| 角色守卫 | 登录后按角色限制接口访问：老板全量 / 收银员仅收银 / 财务仅看账 |
| 多租户隔离 | 所有业务表带 shopId，登录后按身份强制隔离，防止跨店访问 |
| 业务模块 | 菜品 / 桌台 / 结账方式 / 订单 / 看账，接口按角色开放 |

#### 5.4.3 跨服务调用（注册链路）

```
商家 App 提交注册
  → saas-service POST /auth/register（校验手机号唯一）
  → 调 platform-service POST /internal/activation/claim（内网签名）
      ├─ 校验激活码状态（非 unused 拒绝）
      ├─ 建店铺主数据，激活码置为 used + bound_shop_id
      └─ 返回 { success, shopId }
  → saas-service 用 shopId 建老板账号 + 初始配置
  → 返回注册成功，可立即登录
```

> 激活码仅在注册时调用一次，调用量极低，HTTP 直连 + 内网签名足够，无需消息队列。

### 5.5 顾客扫码点餐（项目三，二期）

扫码进店 → 菜单点餐 → 购物车 → 下单 → 我的点单（加菜 / 催菜 / 查看状态）。点餐与接单模式由店铺配置决定。

**本期预留**：商家端服务提供 `GET /public/shops/{shopId}/menu`、`POST /public/orders` 等公开接口设计（匿名访问、按店铺隔离、只读菜单），本期不开发界面。

### 5.6 数据备份（后续排期）

- **云端**：每日定时备份，保留最近 N 天
- **老板手机**：App 退出时增量同步新订单到本地存储，永久留存
- **双保险**：云端与手机各一份，任一方丢失可从另一方恢复

---

## 六、非功能需求

| 类别 | 需求 |
|---|---|
| **性能** | App 运行流畅、资源占用低（原生渲染）；接口响应 < 500ms |
| **安全** | 密码哈希存储；JWT 鉴权；多租户隔离严格；内部接口内网签名；敏感操作记录日志 |
| **可靠** | 云端主源 + 本地备份双保险 |
| **兼容** | 商家 App 支持 Android / iOS；顾客端支持支付宝 / 微信小程序双端（二期） |
| **规范** | API 统一响应格式；命名统一小写下划线；业务状态使用枚举管理 |

---

## 七、技术架构

### 7.1 双服务架构

```
┌───────────────────────────────────────────────────────────────┐
│ platform-service（平台端，NestJS + TypeORM + better-sqlite3）  │
│   数据：platform.db（激活码、店铺主数据，shopId 源头）          │
│   前端：平台后台 Web（激活码 + 店铺信息）                      │
│   权限：运营权限（跨租户看所有店）                              │
└──────────────────────────────┬────────────────────────────────┘
                               │ POST /internal/activation/claim（内网签名）
┌──────────────────────────────▼────────────────────────────────┐
│ saas-service（商家端，NestJS + TypeORM + better-sqlite3）       │
│   数据：saas.db（账号、角色、菜品 / 桌台 / 订单 / 账目）        │
│   前端：商家后台 Web + 收银 App（uni-app x）                    │
│   权限：租户权限（只能看自己店）                                │
└───────────────────────────────────────────────────────────────┘
```

**拆分理由**：平台端管主数据与资费（激活码、店铺档案），商家端管经营数据（菜品、订单、账目），两类数据分属不同库；两个 SQLite 文件（`platform.db` / `saas.db`）各自独立，避开多进程并发写同一 SQLite 的锁冲突。

### 7.2 技术选型

| 端 | 技术 |
|---|---|
| 平台端服务 | Node + NestJS + TypeORM + better-sqlite3 + JWT（内部接口另加签名） |
| 商家端服务 | Node + NestJS + TypeORM + better-sqlite3 + JWT |
| 平台后台 Web / 商家后台 Web | 待定（Vue / React 均可，可与后端同仓或分仓） |
| 商家收银 App | uni-app x（原生渲染） |
| 顾客端 | uni-app（小程序 / H5，二期） |
| 数据库 | SQLite（单文件 × 2，数据量小首期够用） |

### 7.3 多租户隔离实现要点

- 所有业务表带 `shopId`，后端中间件从 JWT 解析店铺并按租户过滤所有查询
- 平台端接口仅限运营身份访问；`/internal/*` 接口仅限内网签名访问
- 测试必须覆盖：A 店 token 访问 B 店数据返回 403 / 空

---

## 八、里程碑

| 阶段 | 内容 | 验证目标 |
|---|---|---|
| **A1 平台端服务** | 激活码实体 + 状态机、批量生成接口、店铺主数据、`/internal/activation/claim` | 平台接口可调通，claim 可建店绑定 |
| **B1 商家端认证** | 注册（跨服务调 claim）、登录 JWT、忘记密码、三角色守卫 | 注册 → 登录 → 按角色访问全链路通 |
| **A2 平台后台页面** | 激活码批量生成表单 + 状态列表 + 店铺列表（可与 B2 并行） | 运营可完成造码、看店 |
| **B2 收银 App** | uni-app x 工程：今日概览、桌台 / 叫号、点餐、接单、结账；老板后台管理（菜品 / 桌台 / 结账方式 / 当日概览 / 范围营业数据） | 员工可独立完成一天营业；老板可 App 内完成高频管理 |
| **B3 商家后台 Web** | 菜品 / 桌台 / 结账方式 / 看账 / 账号 / 店铺设置；老板 App 后台管理复用接口 | 老板可全量维护店铺 |
| **汇合验收** | 全链路：平台造码 → 商家注册开店 → 员工收银一天 → 老板看账；租户隔离测试 | 测试全绿，A 店访问 B 店失败 |

---

## 九、开放问题（待决策）

1. **M3 与 M4 顺序**：收银闭环（B2）与商家后台（B3）的先后——建议先 B2 收银闭环（核心价值），菜品数据用种子数据支撑演示，B3 可后补。
2. **看账范围**：财务需要哪些报表（日 / 周 / 月、菜品排行、结账方式汇总）？首期做哪些？
3. **App 发布方式**：安卓安装包直接下载安装，还是上应用商店？
4. **交班机制**：是否需要交班功能（当班营业额统计 + 交班记录）？
5. **顾客身份**：顾客匿名 + 桌台 / 取餐号，还是未来引入会员体系？（二期）
6. **激活码有效期**：售出激活码是否设有效期 / 过期策略？
7. **Web 前端技术栈**：平台后台与商家后台统一技术栈（同一套组件库）还是各自独立？

---

## 十、决策记录

| 决策 | 结论 | 状态 |
|---|---|---|
| 产品形态 | 三项目：平台方网页后台 / 商家 SaaS / 顾客扫码点餐 | 已定 |
| 平台方激活码 | 自研，归属平台端服务；补运营侧生成 / 管理 | 已定 |
| 后端架构 | 双服务：platform-service（platform.db）+ saas-service（saas.db），跨服务走 HTTP | 已定 |
| 员工 App 权限 | 完整收银（开台 / 点餐 / 接单 / 结账），无后台 | 已定 |
| 后台分工 | 商家后台 Web 全量管理；老板 App 含后台管理（菜品 / 桌台 / 结账方式 / 当日概览 / 范围营业数据） | 已定 |
| 扫码点餐 | 二期，本期预留公开接口设计 | 已定 |
| 商家 App | uni-app x 原生渲染 | 已定 |
| 云端后端 | Node + NestJS + TypeORM + better-sqlite3 | 已定 |
| 支付 | 不接支付，结账 = 记账 | 已定 |
| 角色 | 老板 / 收银员 / 财务，固定三种 | 已定 |
| 点餐 / 接单模式 | 桌台 / 叫号、直接下单 / 接单确认，按店配置 | 已定 |
| 备份 | 云端每日备份 + 老板手机本地永久留存 | 已定 |
