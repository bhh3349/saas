# 平台系统 Logo / Favicon 设计规格档案

> 项目：餐饮收银 SaaS · 平台方管理后台（Web）
> 用途：本档案供设计师参考，交付物用于「系统设置」页上传，全局生效（登录页、侧边栏、浏览器标签页）。

---

## 一、品牌标识体系

平台系统只需要 **1 个主标识**，分 3 种用途复用，不需要单独的横排组合 logo：

| 用途 | 显示位置 | 实际显示尺寸 | 说明 |
|---|---|---|---|
| A. 品牌标（页面 Logo） | 登录页顶部、侧边栏顶部 | 28 × 28 px / 34 × 34 px（圆角方块） | 上传后按方形圆角块裁剪显示 |
| B. Favicon | 浏览器标签页 | 16 × 16 px 及多尺寸 | 极小尺寸下要能识别 |
| C. 源文件（存档） | 设计稿 / 矢量源 | 512 × 512 px（建议 SVG + PNG） | 用于后续扩展与替换 |

**关键点：主标识是「方形小图标」，不是横排 logo。** 页面里它以圆角方块展示，上传后会被自动裁剪为方形，因此图形主体必须居中、四周留安全边距。

---

## 二、用途 A：品牌标（页面 Logo）

- **源文件规格**：512 × 512 px，PNG（透明底）或 SVG（矢量）
- **实际显示**：28 × 28 px（侧边栏）/ 34 × 34 px（登录页），圆角 7~9 px
- **展示方式**：图片以 `object-fit: cover` 填满圆角方块，**超出方块的区域会被裁掉**
- **设计要点**：
  - 图形主体居中，四周留 **≥ 10% 安全边距**（防裁切）
  - 不建议用纯白底（会盖住品牌色底，露出白色方块边框）
  - 建议透明底；颜色与系统品牌色呼应
- **当前品牌色参考**：
  - 主色 `#5E6AD2`（紫蓝渐变，`linear-gradient(135deg, #5E6AD2 → 略深)`）
  - 文字 / 图标白色
  - 背景米白 `#F5F1EB`

## 三、用途 B：Favicon（浏览器标签图标）

- **源文件规格**：512 × 512 px 设计稿，交付多尺寸 PNG + ICO，或 SVG
- **实际显示**：浏览器标签栏 16 × 16 px（高分屏自动放大）
- **建议交付**：
  - `favicon-16.png`（16 × 16）
  - `favicon-32.png`（32 × 32）
  - `favicon-48.png`（48 × 48）
  - `favicon-512.png`（512 × 512，Windows 磁贴用）
  - `favicon.ico`（多尺寸合并：16/32/48）
- **设计要点**：
  - 16 px 下只保留**最简轮廓**，去掉细节（细线、小字、渐变在极小尺寸下会糊）
  - 图形面积占画布 **70%~80%**，四个角不要放关键内容（部分系统会裁圆角）
  - 尽量用纯色块，少用细描边

## 四、上传限制（务必遵守）

| 限制项 | 数值 |
|---|---|
| 单张图片大小 | **≤ 1 MB** |
| 建议格式 | PNG / SVG（也支持 JPG、WebP、ICO） |
| 建议尺寸 | 512 × 512 起步（越大越清晰，上传后按需缩小） |

> 图片以 base64 直接存入数据库，超过 1 MB 会被前端拦截；超大会导致保存失败或后台变慢。

## 五、交付清单（给设计师）

请设计师交付以下文件：

```
logo/
├── 源文件/
│   ├── logo.svg                 # 矢量源（含符号与品牌色定义）
│   └── logo-512.png             # 512×512 透明底，高清导出
├── favicon/
│   ├── favicon-16.png
│   ├── favicon-32.png
│   ├── favicon-48.png
│   ├── favicon-512.png
│   └── favicon.ico              # 16/32/48 多尺寸合并
└── 说明/
    └── 品牌色板.png              # 主色 / 辅助色 / 黑白灰
```

## 六、验收标准

- [ ] Logo 上传后在登录页、侧边栏显示为清晰的圆角方形图标，无变形、无裁切丢失主体
- [ ] Favicon 上传后浏览器标签页清晰可辨（强刷 Ctrl+F5 后生效）
- [ ] 16 × 16 px 下仍能识别图形主体
- [ ] 图片 ≤ 1 MB，透明底优先
