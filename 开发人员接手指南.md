# 餐饮收银 SaaS · 开发人员接手指南

> 核对日期：2026-08-27（基于当前仓库 `main` 分支实际状态编写）
> 适用读者：新接手本仓库的本地开发人员（前端 / 后端）

---

## 0. 文档地图：先读什么

| 文档 | 用途 | 说明 |
|---|---|---|
| `餐饮收银SaaS-PRD.md` | **唯一权威需求文档（v0.3）** | 任何需求 / 接口 / 实体改动必须与 PRD 保持一致；冲突时先同步 PRD 再改代码 |
| **本文档** | 工程接入手册 | 架构、启动、目录、约定、排障 |
| `CODE-WIKI.md` | 历史开发基线 | 部分内容已过时（见 §11 差异说明），以本文档为准 |
| `商家后台Web-UI设计输入文档.md` | 商家后台 Web UI 设计规范 | 配色、间距、组件形态来源 |
| `平台后台Web-UI设计输入文档.md` | 平台后台 UI 设计规范 | 同上 |
| `平台系统LOGO设计规格.md` + `logo/` | LOGO 规范与源文件 | PNG / ICO / SVG 已齐 |
| `收银App开发方向分析.md` | 收银 App（二期）方向 | 当前仓库无收银 App 源码 |
| `platform-service-GO-LIVE-REVIEW.md` | 平台端上线评审 | 上线前逐项核对 |
| `SECURITY-AUDIT-platform-service.md` | 平台端安全审计 | 已修复项与遗留项 |

---

## 0.1 交接范围清单（拿到本文件夹后）

**交付物 = 本文件夹（`d:\c\saas\`）全部内容**，交给接手人后即可继续开发。

**文件夹内包含：**

| 内容 | 位置 |
|---|---|
| 商家后台 Web 源码（主战场） | `merchant-web/`（React 18 + Vite） |
| 平台后台 Web 源码 | `output/platform-admin-web/`（Vue 3，有基础实现） |
| 商家端后端 | `output/saas-service/`（NestJS，saas.db，端口 3200） |
| 平台端后端 | `output/platform-service/`（NestJS，platform.db，端口 3100） |
| 全部文档 | 根目录 10 份 md（PRD / 本指南 / 设计输入 / 审计与上线评审） |
| 部署脚本 | `scripts/deploy/`（01-05 + README） |
| 依赖锁定 | 5 个工程均有 `package-lock.json` |

**接手人 3 步跑起来：**

1. 安装 Node ≥ 18（建议 20.x）；
2. 在 `merchant-web`、`output/saas-service`、`output/platform-service`、`output/platform-admin-web`、`output/saas-admin-web` 五个目录各执行 `npm install`（若交付的是含 `node_modules` 的完整文件夹可跳过）；
3. 两个后端各复制 `.env.example` 为 `.env`，按 §4 启动顺序运行（platform 3100 → saas 3200 → web 5175）。

**文件夹之外需另行准备的事项：**

| 事项 | 说明 |
|---|---|
| 收银 App（uni-app x / Tauri）源码 | **不在本文件夹**，需单独向负责人获取；仓库内仅有《收银App开发方向分析.md》 |
| Node.js 运行时 | 接手人本机安装 |
| Rust / VS Build Tools / `~/.cargo/config.toml` | 仅当继续做 Tauri 桌面收银端时需要（收银 App 源码到位后） |
| `.env` 密钥 | 由接手人从 `.env.example` 生成，不入库 |
| 数据库 `*.db` | 启动后自动创建（saas 用 synchronize，platform 走 migration），不入库 |

---

## 1. 项目是什么

**餐饮收银 SaaS**：云端服务多家餐厅的多租户收银系统。**结账 = 记账，不接支付通道**；平台方自研激活码体系完成商家开通。

PRD 定义三个独立项目：

| 项目 | 端形态 | 使用方 | 仓库现状 |
|---|---|---|---|
| 项目一：平台方网页后台 | Web（Vue 3） | 公司运营 | `output/platform-admin-web`（有基础实现：激活码、店铺） |
| 项目二：商家 SaaS | 网页后台 Web（React）+ 收银 App | 餐厅商家 | Web 即 `merchant-web`（**当前最活跃**）；**收银 App 源码不在本仓库** |
| 项目三：顾客扫码点餐 | 小程序 / H5 | 顾客 | 二期，仅 PRD 预留接口设计 |

**当前开发重点 = 项目二商家后台 Web（`merchant-web`）+ 两个 NestJS 后端。**

---

## 2. 系统架构

```
┌────────────────────────────────────────────────────────────┐
│ 平台后台 Web (Vue3)   output/platform-admin-web    :5173    │
│   └─ /auth /admin /codes /shops → 平台服务                    │
│                                                             │
│ 商家后台 Web (React18)  merchant-web               :5175    │
│   └─ /auth /admin /reports /tables → 商家服务                 │
└───────────────┬───────────────────────┬────────────────────┘
                │ HTTP + JWT            │ HTTP + JWT
        ┌───────▼────────┐      ┌───────▼────────┐
        │ platform-service│      │ saas-service   │
        │ NestJS  :3100   │      │ NestJS  :3200  │
        │ platform.db     │      │ saas.db        │
        │ 激活码/店铺/运营  │◄────►│ 账号/菜品/桌台   │
        │ 运营(跨租户)     │      │ 订单/账目/报表   │
        └─────────────────┘      └────────────────┘
           /internal/claim            /internal/*（内网签名）
```

**核心设计原则（硬约束）：**

1. **双 SQLite 文件**：`platform.db`（平台端）、`saas.db`（商家端），各自独立，避免多进程并发写同一 SQLite 的锁冲突。
2. **禁止跨服务直连数据库**：跨服务只走内部 HTTP 接口 `/internal/*`，携带**共享密钥 + 时间戳签名**鉴权。
3. **注册链路**：商家 App → saas-service `POST /auth/register`（校验手机号唯一）→ 调 platform-service `POST /internal/activation/claim {code, shopName, phone}`（校验激活码、建店铺、绑定、返回 `{success, shopId}`）→ saas-service 用 shopId 建老板账号 + 初始配置。
4. **激活码状态机**：`unused → used`（注册绑定）；`unused → void`（作废）；`used / void` 不可再变更。

**多租户隔离**：所有业务表带 `shopId`；后端从 JWT 解析 `userId / shopId / role`，查询强制按租户过滤；平台端接口仅运营身份访问；`/internal/*` 仅内网签名访问。

**端口速查：**

| 服务 | 端口 | 说明 |
|---|---|---|
| platform-service（后端） | **3100** | 激活码、店铺主数据、运营账号 |
| saas-service（后端） | **3200** | 商家全量业务 + 报表 |
| merchant-web（Vite dev） | **5175** | 商家后台 Web，proxy → 3200 |
| platform-admin-web（Vite dev） | **5173** | 平台后台 Web，proxy → 3100 |

---

## 3. 仓库目录总览

```
d:\c\saas\
├── 餐饮收银SaaS-PRD.md             # 唯一权威需求文档（v0.3）
├── CODE-WIKI.md                    # 历史基线（部分过时）
├── 开发人员接手指南.md              # 本文档
├── merchant-web/                   # ★ 商家后台 Web（React 18 + Vite，主战场）
│   ├── src/
│   │   ├── App.tsx                 # 主框架：导航/多页签/hash 路由/星标收藏/主题
│   │   ├── data/navigation.ts      # 菜单与 ViewKey 定义（唯一路由表）
│   │   ├── api/                    # 后端接口封装（14 个模块）
│   │   ├── components/             # 自研组件（29 个）
│   │   ├── views/                  # 页面（33 个，含 reports/ 12 个报表页）
│   │   ├── styles/global.css       # 全局样式与设计变量
│   │   ├── theme.ts                # 黑白主题切换
│   │   └── utils/                  # favorites.ts（星标收藏）、excel.ts（导入）
│   ├── vite.config.ts              # 5175 + proxy → 3200
│   └── package.json                # React 18 / Vite 5 / TS 5.6
├── output/
│   ├── saas-service/               # ★ 商家端服务（NestJS，saas.db）
│   ├── platform-service/           # ★ 平台端服务（NestJS，platform.db）
│   ├── platform-admin-web/         # 平台后台 Web（Vue3，有基础实现）
│   └── saas-admin-web/             # Vue3 脚手架空壳（占位，无业务）
├── platform-service-GO-LIVE-REVIEW.md   # 平台端上线评审
├── SECURITY-AUDIT-platform-service.md   # 平台端安全审计
├── data-table-template.md          # 数据表格统一模板
├── frontend/                       # 静态原型页（index.html / merchant-login.html，非产品源码）
├── scripts/                        # deploy/ 部署脚本、deploy_remote.py
├── logo/                           # LOGO 源文件 / favicon / 说明
├── start-merchant.ps1              # Windows 一键重启：platform+saas+merchant-web
├── restart-saas.ps1                # 只重启 saas-service（build + 起服务）
├── rebuild-restart.ps1             # 双后端 rebuild + restart + 健康检查
├── copy_ref.ps1 / parse_xlsx.py    # 历史辅助脚本（可忽略）
├── seed_area.js / revert_area.js   # 桌台区域 seed/回滚 辅助脚本
└── login.yaml                      # 登录页设计稿标注（设计输入，非代码）
```

---

## 4. 快速开始（15 分钟内跑起来）

### 4.1 环境要求

| 依赖 | 版本要求 | 说明 |
|---|---|---|
| Node.js | ≥ 18（建议 20.x） | 未提供 `.nvmrc`，以本机稳定版为准 |
| npm | 随 Node | 各工程独立安装依赖 |
| Windows | — | 本仓库以 Windows + PowerShell 开发为主 |

### 4.2 首次安装

```powershell
# 1) 商家端服务
cd d:\c\saas\output\saas-service
npm install

# 2) 平台端服务
cd d:\c\saas\output\platform-service
npm install

# 3) 商家后台 Web
cd d:\c\saas\merchant-web
npm install
```

### 4.3 环境变量

两个后端各自 `npm install` 后，从 `.env.example` 复制为 `.env`（开发环境可先用示例默认值）：

```powershell
cd d:\c\saas\output\platform-service
Copy-Item .env.example .env
cd ..\saas-service
Copy-Item .env.example .env
```

关键变量（开发默认即可，**生产必须改**）：

- **platform-service**：`PORT=3100`、`DB_PATH=data/platform.db`、`DB_SYNC=false`（生产走 migration）、`SEED_OPERATOR_USERNAME=admin`、`SEED_OPERATOR_PASSWORD`（**留空则启动时自动生成随机密码并打印一次日志**）、`JWT_SECRET`、`INTERNAL_SHARED_SECRET`、`SAAS_INTERNAL_SECRET`、`CORS_ORIGINS`。
- **saas-service**：`PORT=3200`、`DB_PATH=data/saas.db`、`JWT_SECRET=dev-jwt-secret-change-me`、`PLATFORM_INTERNAL_BASE_URL=http://127.0.0.1:3100`、`PLATFORM_INTERNAL_SECRET`（**必须与 platform-service 的 `INTERNAL_SHARED_SECRET` 一致**）、`DEFAULT_TABLE_COUNT=10`。

### 4.4 启动（开发模式）

```powershell
# 终端 1：平台端服务（3100）
cd d:\c\saas\output\platform-service
npm run start:dev

# 终端 2：商家端服务（3200）
cd d:\c\saas\output\saas-service
npm run start:dev

# 终端 3：商家后台 Web（5175，自动打开浏览器）
cd d:\c\saas\merchant-web
npm run dev
```

> 说明：saas-service `synchronize: true` 自动建表；platform-service 受 `DB_SYNC` 控制（生产建议 false + migration）。

### 4.5 一键脚本（Windows，编译产物模式）

- `start-merchant.ps1`：杀掉 3100 / 3200 / 5175 占用进程 → 分别以 `dist/main.js`（需先 build）与 `npx vite` 重启 → curl 健康检查。
- `rebuild-restart.ps1`：`npm run build` 两个后端 → 重启 3100 / 3200 → 健康检查。
- `restart-saas.ps1`：只重建并重启 saas-service。

> 先 `npm run build` 双后端，再执行上述脚本即可用"编译产物 + 隐藏窗口"方式跑起来（适合日常联调）。
> 注意：PowerShell 里命令行直接传含 `$` 的变量会被外层转义破坏，复杂命令写成 `.ps1` 后 `-File` 执行。

### 4.6 演示账号与数据

- **平台端运营**：`admin` / 密码来源 = `.env` 的 `SEED_OPERATOR_PASSWORD`（未配置则启动日志打印随机密码，仅一次）。
- **商家端**：无预置演示账号，走**注册新店铺**流程 —— 需要一枚**激活码**。
  1. 平台后台 `output/platform-admin-web`（Vue，5173）登录运营账号 → 「激活码管理」生成一枚激活码（或直接插库）；
  2. 商家后台 `merchant-web`（5175）登录页点「注册新店铺」→ 填激活码 + 店铺名 + 手机号 + 密码 → 注册成功后即为**老板**账号。
- **本地数据文件**：`output/platform-service/data/platform.db`、`output/saas-service/data/saas.db`（均已 gitignore，不入库；删掉可重置环境）。

---

## 5. 工程详解

### 5.1 merchant-web（商家后台 Web，主战场）

**技术栈**：React 18 + TypeScript 5.6 + Vite 5 + dayjs + xlsx（菜品批量导入）+ antd（少量，多数为自研组件）。样式为 **CSS 变量 + 自研组件体系**，无 Tailwind。

**导航系统（务必先理解）**：

- 路由表唯一定义在 `src/data/navigation.ts`：`NAV_GROUPS`（两大分组 `ops` 运营中心 / `rpt` 报表中心）→ 菜单项 → `ViewKey`（如 `ops:dish:library`、`rpt:biz-stats`）。
- 路由是 **hash 路由**：`#/ops/dish/library` ↔ ViewKey（`App.tsx` 的 `viewKeyToHash` / `hashToViewKey`），支持手动改 URL / 前进后退 / 直达深链。
- **多页签系统**：`TabsBar` 展示已打开子页，页签状态持久化到 localStorage（`merchant_current_view` / `_group` / `_page` / `_tabs`），刷新后原样恢复。
- **星标收藏**：每个子页面右上角五角星（`FavStar` 组件包一层 `FavPage`），点亮后加入对应首页的「快捷入口」。收藏数据在 `utils/favorites.ts`，首页（`OpsHome` / `ReportHome`）的快捷入口面板动态读取。
- **主题**：`theme.ts` 黑白主题切换，CSS 变量驱动。

**目录：**

```
src/
├── App.tsx                  # 主框架（登录分流/渲染/页签/hash/收藏/主题）
├── data/navigation.ts       # 菜单 + ViewKey 路由表
├── api/                     # HTTP 封装 + 各业务 API
│   ├── http.ts              # fetch 封装：baseURL 走 vite proxy、token、统一 {code,message,data}
│   ├── types.ts             # 后端返回类型（MerchantUser 等）
│   ├── auth.ts / dishes.ts / categories.ts / attributes.ts / setmeals.ts
│   ├── buckets.ts           # 配置桶整包读写（print/stall/voucher/discount/...）
│   ├── tables.ts / areas.ts / payments.ts / orders.ts / reports.ts
├── components/              # 29 个自研组件
│   ├── TopBar / Drawer / TabsBar / FavStar / Icon / EmptyState / Pagination / Modal
│   ├── BatchImportDishModal / SelectDishesModal / DishPickerModal
│   ├── SortDishModal / SortAreaModal          # 排序弹窗
│   └── report/                                # TrendChart 等报表图表组件
├── views/                   # 33 个页面（含 reports/ 子目录 12 个）
│   ├── AuthScreen.tsx       # 登录/注册/忘记密码
│   ├── OpsHome.tsx          # 运营中心首页（概览 + 快捷入口）
│   ├── ReportHome.tsx       # 报表中心首页（趋势 + 收入构成 + 快捷入口）
│   ├── TableManage / CheckoutManage / VoucherManage / DiscountManage
│   ├── PrintAssign / StallManage / MustDish / BusinessMode
│   ├── DishLibrary / DishCategory / DishAttribute
│   ├── StoreProfile / StaffManage / RoleManage
│   ├── BizStats.tsx         # 综合营业统计
│   └── reports/             # PromoStats / AreaTableStats / CompareReport /
│                            # DishSalesStats / DishDiscountStats / DishDetail /
│                            # DishRefundStats / InStoreOrders / SensitiveStats /
│                            # SensitiveDetail / IncomeDiscountStats /
│                            # IncomeCouponStats / IncomeDiscountDetail
├── styles/global.css        # 全部样式与设计变量
├── theme.ts
└── utils/                   # favorites.ts / excel.ts
```

**数据架构（2026-08 已从 localStorage 全量迁移到后端）**：

| 数据类别 | 存储位置 | 说明 |
|---|---|---|
| 菜品 / 分类 / 属性 / 套餐 | 后端实体表（`/admin/dishes`、`/admin/categories`、`/admin/attributes?kind=spec\|method\|unit`、`/admin/setmeals`） | id 后端自增，页面内 `String(id)` |
| 9 个配置类模块 + 结账方式 | `shop_buckets` 桶表整包 JSON（keys：`print`/`stall`/`voucher`/`discount`/`mustdish`/`role`/`staff`/`store_profile`/`business_settings`/`payments`） | 加载 `getBucket` 有数据用之否则本地默认；保存 `putBucket` 整包 |
| UI 偏好 | localStorage | 仅 theme、tab、列设置、视图布局、认证（token/user/shop） |

**前端表格规范（所有数据页统一模板）**：模板在根目录 `data-table-template.md`，参考实现 `merchant-web/src/views/DishLibrary.tsx`。要点：`.data-table.table-list > .area-table-scroll.checkout-scroll > table.checkout-real-table`；表头 sticky + 操作列冻结（th-sticky / td-sticky）；状态用 `.status-tag.status-on/off`；空态用 `EmptyState` 且 `colSpan=总列数`；分页用统一 `Pagination` 组件（默认 10 条/页，可选 10/20/30/50）。弹窗统一 `.modal-mask > .modal-card`。

**报表中心（rpt 分组）**：14 个页面，数据来自 saas-service `reports` 模块（本地 `orders` / `order_refunds` 表聚合），支持日期范围/结账方式/门店维度筛选，金额单位「元」。

### 5.2 saas-service（商家端服务）

**技术栈**：NestJS + TypeORM + better-sqlite3（**同步 API**）+ @nestjs/jwt + bcryptjs。

**模块（13）**：auth、staff、dishes、categories、attributes、setmeals、buckets、tables、areas、payments、orders、reports、internal。

**实体（13）**：User、Table、Area、PaymentMethod、Dish、Order、OrderRefund、Shop、Category、Attribute、Setmeal、ShopBucket、OperationLog。

**要点**：

- 统一响应 `{ code, message, data }`；错误用业务错误码。
- JWT 携带 `userId / shopId / role`；角色：老板（全量）/ 收银员（仅收银）/ 财务（仅看账，写接口拒绝）。
- 多租户：业务表带 `shopId`，查询强制过滤；A 店 token 访问 B 店数据返回 403 / 空（**测试必须覆盖**）。
- 全局守卫 `ShopStatusGuard`：店铺停用（disabled）全局拦截返回 403。
- 敏感操作（作废激活码、改价、删桌台等）写 `OperationLog`（平台端为 `OpLog`）。
- **金额约定**：后端实体以「分」存整数（套餐 price、套餐分组内 dish price）；菜品 / 折扣等 API 层用「元」。前后端传参务必分清。
- 套餐分组结构：后端 DTO 无 `required/default/amount` 字段且 whitelist 会剥离，提交时不要传这些字段。
- `synchronize: true`（开发自动建表），开发期方便；生产建议 migration。

### 5.3 platform-service（平台端服务）

**技术栈**：同 saas-service（NestJS + TypeORM + better-sqlite3 + JWT + bcrypt12）。

**模块（7）**：auth、codes（激活码）、shops（店铺主数据）、internal、op-log、saas-client、settings。

**实体（5）**：Operator、ActivationCode、Shop、OpLog、SystemSetting。

**要点**：

- **激活码状态机**：`unused → used`（注册绑定）/ `unused → void`（作废）；`used / void` 不可再变更。
- **种子运营账号**：启动时按 `SEED_OPERATOR_USERNAME` 自动创建 admin（bcrypt cost 12）；密码来自 `SEED_OPERATOR_PASSWORD`，未配置则随机生成并**仅在启动日志打印一次**。
- **内部接口签名**：`/internal/*` 用共享密钥 + 时间戳签名（`INTERNAL_SHARED_SECRET`），与 saas-service 的 `PLATFORM_INTERNAL_SECRET` 对齐。
- **全局限流**：Throttler 默认 60 次/分钟，登录 / 改密在 controller 单独收紧。
- **DB_SYNC 默认 false**：schema 变更走 `npm run migration:generate` / `migration:run`。
- 安全审计与上线评审：见 `SECURITY-AUDIT-platform-service.md`、`platform-service-GO-LIVE-REVIEW.md`（已覆盖：密钥强度、bcrypt、限流、激活码幂等、操作审计等）。

### 5.4 其余工程（了解即可）

| 工程 | 状态 | 说明 |
|---|---|---|
| `output/platform-admin-web` | 有基础实现 | Vue 3 + Vite（5173，proxy→3100）。视图：Login / Codes（激活码管理）/ Shops（店铺管理）/ Settings / App 壳。运营内部使用 |
| `output/saas-admin-web` | 空壳 | Vue 3 脚手架，仅 Login / Placeholder，无业务 |
| `frontend/` | 原型 | 2 个静态 HTML（index / merchant-login），非产品代码 |
| 收银 App（uni-app x / Tauri） | **不在本仓库** | 仅《收银App开发方向分析.md》设计文档；需另行获取源码分支 |

---

## 6. 关键业务与编码约定（后端规范，新增代码必须遵守）

1. **技术栈**：Node + NestJS + TypeORM + better-sqlite3 + JWT。better-sqlite3 是同步 API：`SELECT` 用同步查询，**不要用 async/await 包数据库操作**。
2. **密码必须哈希存储**（bcrypt 或 argon2），禁止明文。
3. **统一响应格式**：`{ code, message, data }`；业务错误用错误码而非滥用 HTTP 状态。
4. **命名统一小写下划线**（字段、路径、数据库列名）；DTO 用 `class-validator` 校验。
5. **业务状态用枚举管理**（TS enum / union），禁止魔法字符串散落。
6. **认证与角色**：JWT 携带 `userId / shopId / role`；角色守卫：老板全量 / 收银员仅收银 / 财务仅看账。
7. **多租户**：所有业务表带 `shopId`，查询必须带租户条件，禁止跨店访问。
8. **敏感操作记录操作日志**。
9. 模块化目录：`src/modules/<业务>/` + `src/common/`（守卫/中间件/拦截器/枚举）+ `src/config/`。

**前端约定：**

- 所有数据表格复用统一模板（§5.1）；分页默认 10 条/页；排序通过顶部「排序」弹窗（上移/下移），操作列只留编辑/删除。
- 金额展示：报表「元」；提交菜品/折扣用「元」；套餐分组内 dish price 用「分」（后端约束）。
- 颜色体系：表格主体 `var(--color-surface2)` #F5F1EB，表头/操作列 `var(--color-surface3)` #EDE7DF，页面背景纯白；主色 `var(--color-primary)`。不要混用其他颜色。

---

## 7. 常用命令

```powershell
# merchant-web
npm run dev          # 5175
npm run build        # tsc -b && vite build（产物 dist/，含 .gz 预压缩）

# saas-service
npm run start:dev    # 开发热更
npm run build        # tsc 编译到 dist/
npm run smoke        # 冒烟测试（脚本 scripts/smoke.mjs：注册→登录→CRUD→报表链路）

# platform-service
npm run start:dev
npm run build
npm run migration:generate / migration:run   # 生产 schema 变更
```

**冒烟测试**：`output/saas-service/scripts/smoke.mjs` 会走一遍注册链路（含 platform-service internal 调用），是验证双服务联通最快的方式。

---

## 8. Git 与代码托管

| 项 | 内容 |
|---|---|
| 远程仓库（origin） | `https://github.com/bhh3349/saas.git`（GitHub） |
| 默认分支 | `main`（唯一主干分支，本地与 origin 同步） |
| 工作流 | 直接提交 `main`；功能开发建议先拉分支再合回 |
| 提交规范 | Conventional Commits：`feat(scope):` / `fix(scope):` / `chore:` / `docs:` / `refactor:`（历史提交已按此风格，新提交保持一致） |
| 提交信息语言 | 中英文均可，历史以中文为主；Windows 控制台直接写中文提交信息可能乱码，建议用 `git commit` 编辑器或先设 `core.quotepath=false` |
| 忽略清单 | 数据库文件（`*.db`）、`node_modules/`、`dist/`、构建产物、日志、`.env` 均不入库；**密钥绝不提交** |

## 9. 线上部署记录

### 9.1 部署形态（当前线上）

| 项 | 内容 |
|---|---|
| 云厂商 / 系统 | 腾讯云 · Ubuntu 22.04 / 24.04 |
| 服务器地址 / SSH | `140.143.201.204` · 用户 `root`（端口 22） |
| 服务器目录 | `/opt/saas/`（scripts / output / backups / logs） |
| 进程管理 | pm2（`pm2 startup` 开机自启），双服务各一进程 |
| 反向代理 / 静态托管 | Nginx（反代 3100 / 3200 + 托管前端静态文件） |
| 数据备份 | 每日凌晨 2 点 crontab，保留 30 天（`scripts/deploy/05-backup.sh`） |
| 软件源适配 | 腾讯云内网 apt 镜像 + npmmirror，不依赖国际网络 |

> 说明：**无域名**，线上经 `http://140.143.201.204` 访问（nginx 监听 80 反代到前端静态资源与 3100 / 3200 后端）。该 IP 已在 GitHub 仓库中记录，注意避免开放不必要端口；22 建议改用密钥登录并禁用密码。

### 9.2 部署步骤（脚本化）

```bash
# 服务器 /opt/saas/scripts 下依次执行：
bash 01-apt-tencent.sh    # apt 换腾讯云内网源 + 基础软件
bash 02-install-node.sh   # Node 22（npmmirror 国内镜像）
bash 03-deploy.sh         # 装依赖 + 构建 + 生成 .env + pm2 启动双服务
bash 04-nginx.sh          # Nginx 反代 + 静态托管
bash 05-backup.sh         # 每日备份 crontab
```

完整说明见 `scripts/deploy/README.md`。

### 9.3 生产部署要点

- `.env` 全部密钥改为 ≥32 字节随机 hex；`JWT_SECRET` / `INTERNAL_SHARED_SECRET` / `SAAS_INTERNAL_SECRET` 三把钥匙分开设置（03 脚本会自动生成随机值并保证三处一致）。
- platform-service：`DB_SYNC=false` + migration；`CORS_ORIGINS` 收紧到实际域名。
- 前端产物：`merchant-web` build 出 `dist/`（含 `.gz`），配合 nginx 静态托管；proxy 改由 nginx 反代到 3200 / 3100。
- 上线前逐项核对 `platform-service-GO-LIVE-REVIEW.md`。
- 验证：`pm2 status` 两个进程 online；`curl` 打登录接口冒烟。

---

## 10. 常见问题排查

| 症状 | 排查方向 |
|---|---|
| 前端能开但接口 404 | 确认 saas-service(3200) 已启动；vite proxy 仅覆盖 `/auth /admin /tables /reports`，新增前缀记得加 |
| 注册提示激活码无效 | 激活码在 platform.db（`activation_codes` 表）；确认已生成且状态 unused；注意两服务 `INTERNAL_SHARED_SECRET` / `PLATFORM_INTERNAL_SECRET` 必须一致 |
| 平台端登录失败 | 种子账号只有 `admin`；密码看启动日志（未设 `SEED_OPERATOR_PASSWORD` 时随机生成仅打印一次），忘记则删库重启 |
| 数据表结构变了不生效 | saas-service 删库重启（synchronize 建新表）；platform-service 走 migration |
| 端口被占用 | `Get-NetTCPConnection -LocalPort 3100/3200/5175 -State Listen` 查占用进程 |
| PowerShell 传参异常（`$` 被吞） | 写成 `.ps1` 文件后 `-File` 执行，勿在命令行内嵌变量表达式 |
| 改了 entities 但 TypeORM 报错 | better-sqlite3 是同步驱动，检查是否误用 async 包裹 DB 操作 |
| 套餐提交字段被剥离 | 后端 DTO whitelist 会剥 `required/default/amount`，前端别传 |
| 金额多了/少了 100 倍 | 检查是否在「分 / 元」之间换算错误（实体存分，API 层元，套餐分组内 dish price 为分） |

---

## 11. 现状与待办（接手前必须知道）

1. **收银 App（uni-app x）源码不在本仓库**；如需开发需向负责人获取对应代码库 / 分支。
2. `output/saas-admin-web` 是空壳脚手架，项目二商家后台以 `merchant-web` 为准。
3. `CODE-WIKI.md` 写于早期（设计稿转码 + localStorage 阶段），其中"纯前端 localStorage 数据"、"19 个页面"、"antd 组件体系"等描述**已过时**，以本文档 §5 与 `data/navigation.ts` 实际为准。
4. `frontend/`、`login.yaml`、`copy_ref.ps1`、`parse_xlsx.py`、`seed_area.js`、`revert_area.js` 为历史辅助产物，非产品代码。
5. 商家端角色仅固定三种（老板 / 收银员 / 财务），不开放自定义。
6. 数据表变更时，同步更新 PRD 与本文档对应章节，保证"文档 ↔ 代码"一致。

---

## 12. 开发约定与踩坑记录（实战沉淀，2026-08 确认）

> 以下规则是多次开发迭代后确认下来的**硬约定**，新增 / 修改页面时必须遵守，与 §6 编码规范互为补充。

### 12.1 前端页面规范（已全面落地）

| 主题 | 规则 |
|---|---|
| 表格结构 | `.data-table.table-list > .area-table-scroll.checkout-scroll > table.checkout-real-table`；表头 sticky；行 hover 用 `--row-hover` |
| 操作列冻结 | 表头 `th-sticky` + 数据行 `td-sticky`（global.css 已有 `position: sticky; right: 0` + 左描边），横向滚动钉在右侧 |
| 操作按钮 | `.row-actions`（gap 16px），`a.link` / `button.action-link`，无竖线分隔；危险操作加 `.danger` |
| 操作列宽度 | `按钮数 × 44 + (按钮数-1) × 16`，如 2 个按钮 = 104px，4 个按钮 = 224px |
| 其余列 | colgroup 每列 `width: 150` 等宽均分，内容过长自适应撑开 |
| 分页 | 统一 `components/Pagination.tsx`（`DEFAULT_PAGE_SIZE=10`，选项 10/20/30/50）；筛选/删除后做页码越界保护；切换 tab / 搜索重置 page=1；弹窗列表不翻页 |
| 排序 | 页面有「排序」按钮时（打开排序弹窗上移/下移），表格操作列**只留编辑/删除**，不放上移/下移 |
| 删除 | 无"默认项不可删"限制；确认弹窗统一文案（关联菜品 > 0 提示关联失效，否则提示不可恢复） |
| 状态标签 | `.status-tag.status-on` / `.status-off`；空态用 `EmptyState` 且 `colSpan=总列数` |
| 弹窗 | `.modal-mask > .modal-card`（`.modal-head > .modal-title + .modal-close`；`.modal-foot > .tm-btn.tm-btn-default/primary`） |
| 颜色 | 表格米白 `var(--color-surface2)` #F5F1EB，表头/操作列 `var(--color-surface3)` #EDE7DF，页面背景纯白，主色 `var(--color-primary)`；**禁止混用其他颜色** |

### 12.2 后端数据契约（易踩）

- **金额单位**：实体层「分」存整数；API 层「元」；套餐分组内 dish price 按「分」传。前端提交时务必区分。
- **套餐分组 DTO**：后端无 `required/default/amount` 字段且 whitelist 会剥离，**提交时不要传这些字段**。
- **配置桶**：`shop_buckets` 整包 JSON，keys = `print` / `stall` / `voucher` / `discount` / `mustdish` / `role` / `staff` / `store_profile` / `business_settings` / `payments`；加载有数据用之、无则本地默认，保存整包覆盖。
- **多租户**：所有业务表带 `shopId`，查询强制过滤；测试必须覆盖" A 店 token 访问 B 店数据 → 403 / 空"。

### 12.3 2026-08 关键架构决策（背景，改前必读）

1. 商家后台数据已从 **localStorage 全量迁移到后端**：菜品/分类/属性/套餐走实体表；9 个配置模块 + 结账方式走 `shop_buckets` 桶；localStorage 仅剩 UI 偏好与认证（token/user/shop）。**不要回退到纯前端存储**。
2. 报表中心新增 `rpt` 导航分组 + 快捷入口，首页（`OpsHome` / `ReportHome`）的快捷入口从 `utils/favorites.ts` 动态读取星标收藏。
3. 导航用 ViewKey + hash 路由（`#/ops/dish/library`），新增页面 = `data/navigation.ts` 注册 + `App.tsx` 挂渲染，勿直接改路由库。

### 12.4 Windows 本地开发环境坑

- **PowerShell `$` 转义**：命令行内嵌含 `$` 的变量表达式会被外层 shell 破坏，复杂命令写成 `.ps1` 后 `-File` 执行。
- **端口占用**：3100 / 3200 / 5175 被占 → `Get-NetTCPConnection -LocalPort <port> -State Listen` 定位进程。
- **better-sqlite3 同步 API**：数据库操作不要用 async/await 包装。
- **历史经验（仅供参考，收银 App 源码不在本仓库）**：Tauri 编译需 `[profile.dev] debug=false` 否则 Windows 沙箱 OOM；编译用 `cargo build -j 2`。

### 12.5 新增功能标准流程（checklist）

1. 需求改动先同步 `餐饮收银SaaS-PRD.md`（唯一权威），再写代码；
2. 后端：新模块按 `src/modules/<业务>/` 创建，DTO 用 class-validator，实体带 `shopId`，写操作记日志，金额按分/元约定；
3. 前端：页面在 `data/navigation.ts` 注册 ViewKey → `App.tsx` 挂渲染 → 列表页复用 §12.1 表格模板 → 分页/排序/删除按 §12.1 规则；
4. 联调：双服务 `npm run start:dev` + `merchant-web` 5175；注册链路走 smoke.mjs；
5. 收尾：更新 PRD（如需）、本文档对应章节、`CODE-WIKI.md` 标注过时项。
