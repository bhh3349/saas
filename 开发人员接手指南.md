# 餐饮收银 SaaS · 开发人员接手指南

> 核对日期：2026-08-28（基于当前仓库 `main` 分支实际状态编写）；2026-09-06 更新部署实况（§9）与收银 App 待办（§11.9）
> 适用读者：新接手本仓库的本地开发人员（前端 / 后端 / App）

---

## 0. 文档地图：先读什么

| 文档 | 用途 | 说明 |
|---|---|---|
| `餐饮收银SaaS-PRD.md` | **唯一权威需求文档（v0.3）** | 任何需求 / 接口 / 实体改动必须与 PRD 保持一致；冲突时先同步 PRD 再改代码 |
| **本文档** | 工程接入手册 | 架构、启动、目录、约定、排障 |
| `收银App开发接手指南.md` | **收银 App（uni-app x）专项接入** | 工程结构、HBuilderX 环境、本地编译、打印插件、完整业务闭环 |
| `CODE-WIKI.md` | 历史开发基线 | 部分内容已过时（见 §11 差异说明），以本文档为准 |
| `商家后台Web-UI设计输入文档.md` | 商家后台 Web UI 设计规范 | 配色、间距、组件形态来源 |
| `平台后台Web-UI设计输入文档.md` | 平台后台 UI 设计规范 | 同上 |
| `平台系统LOGO设计规格.md` + `logo/` | LOGO 规范与源文件 | PNG / ICO / SVG 已齐 |
| `收银App开发方向分析.md` | 收银 App 技术选型分析（uts vs Tauri） | **最终选型：uni-app x（UTS 原生渲染）**，源码已在仓库 `cashier-app/` |
| `platform-service-GO-LIVE-REVIEW.md` | 平台端上线评审 | 上线前逐项核对 |
| `SECURITY-AUDIT-platform-service.md` | 平台端安全审计 | 已修复项与遗留项 |

---

## 0.1 交接范围清单（拿到本文件夹后）

**交付物 = 本文件夹（`d:\c\saas\`）全部内容**，交给接手人后即可继续开发。

**文件夹内包含：**

| 内容 | 位置 |
|---|---|
| 商家后台 Web 源码（主战场） | `merchant-web/`（React 18 + Vite） |
| 收银 App 源码（uni-app x） | `cashier-app/`（UTS 原生渲染，蓝牙 + WiFi TCP 打印） |
| 平台后台 Web 源码 | `output/platform-admin-web/`（Vue 3，有基础实现） |
| 商家端后端 | `output/saas-service/`（NestJS，saas.db，端口 3200） |
| 平台端后端 | `output/platform-service/`（NestJS，platform.db，端口 3100） |
| 全部文档 | 根目录 12 份 md（PRD / 本指南 / 收银 App 指南 / 设计输入 / 审计与上线评审） |
| 部署脚本 | `scripts/deploy/`（01-05 + README） |
| 依赖锁定 | 5 个工程均有 `package-lock.json`（cashier-app 非 npm 工程，无 package.json） |

**接手人 3 步跑起来：**

1. 安装 Node ≥ 18（建议 20.x）；
2. 在 `merchant-web`、`output/saas-service`、`output/platform-service`、`output/platform-admin-web` 四个目录各执行 `npm install`（若交付的是含 `node_modules` 的完整文件夹可跳过）；
3. 两个后端各复制 `.env.example` 为 `.env`，按 §4 启动顺序运行（platform 3100 → saas 3200 → web 5175）。
4. 收银 App（cashier-app）另需 HBuilderX + JDK + Android SDK，详见 §4.7 与《收银App开发接手指南.md》。

**文件夹之外需另行准备的事项：**

| 事项 | 说明 |
|---|---|
| Node.js 运行时 | 接手人本机安装 |
| HBuilderX 5.x（uni-app x 必需） + DCloud 账号（绑手机号） | 收银 App 本地编译运行必需；账号已在 manifest.json 生成 appid = `__UNI__539B9A9`，接手人需登录同一账号或重新获取 AppID |
| JDK 17 + Android SDK 34 + Gradle 8.11.1 | 收银 App 本地编译依赖 |
| MuMu 模拟器（默认端口 16384）或 Android 真机 + adb | 收银 App 运行调试 |
| 真实 ESC/POS 热敏打印机（蓝牙 / WiFi） | 打印功能真机联调必需；模拟器仅能测试 UI 流程与错误处理 |
| Rust / VS Build Tools / `~/.cargo/config.toml` | 仅当切换为 Tauri 桌面收银端时需要（当前选型为 uni-app x，不需要） |
| `.env` 密钥 | 由接手人从 `.env.example` 生成，不入库 |
| 数据库 `*.db` | 启动后自动创建（saas 用 synchronize，platform 走 migration），不入库 |

---

## 1. 项目是什么

**餐饮收银 SaaS**：云端服务多家餐厅的多租户收银系统。**结账 = 记账，不接支付通道**；平台方自研激活码体系完成商家开通。

PRD 定义三个独立项目：

| 项目 | 端形态 | 使用方 | 仓库现状 |
|---|---|---|---|
| 项目一：平台方网页后台 | Web（Vue 3） | 公司运营 | `output/platform-admin-web`（有基础实现：激活码、店铺） |
| 项目二：商家 SaaS | 网页后台 Web（React）+ 收银 App（uni-app x） | 餐厅商家 | Web 即 `merchant-web`（当前最活跃）；**收银 App 源码 `cashier-app/`（UTS 原生渲染，完整闭环）** |
| 项目三：顾客扫码点餐 | 小程序 / H5 | 顾客 | 二期，仅 PRD 预留接口设计 |

**当前开发重点 = 项目二（merchant-web + cashier-app）+ 两个 NestJS 后端。**

---

## 2. 系统架构

```
┌────────────────────────────────────────────────────────────┐
│ 平台后台 Web (Vue3)   output/platform-admin-web    :5173    │
│   └─ /auth /admin /codes /shops → 平台服务                    │
│                                                             │
│ 商家后台 Web (React18)  merchant-web               :5175    │
│   └─ /auth /admin /reports /tables → 商家服务                 │
│                                                             │
│ 收银 App (uni-app x)  cashier-app                 Android   │
│   └─ /auth /areas /tables /dishes /orders /reports          │
│      /payments /printers → 商家服务 (saas-service:3200)      │
└───────────────┬───────────────────────┬────────────────────┘
                │ HTTP + JWT            │ HTTP + JWT
        ┌───────▼────────┐      ┌───────▼────────┐
        │ platform-service│      │ saas-service   │
        │ NestJS  :3100   │      │ NestJS  :3200  │
        │ platform.db     │      │ saas.db        │
        │ 激活码/店铺/运营  │◄────►│ 账号/菜品/桌台   │
        │ 运营(跨租户)     │      │ 订单/账目/报表   │
        └─────────────────┘      └────────────────┘
           /internal/claim            /internal/*（内网签名）
```

**核心设计原则（硬约束）：**

1. **双 SQLite 文件**：`platform.db`（平台端）、`saas.db`（商家端），各自独立，避免多进程并发写同一 SQLite 的锁冲突。
2. **禁止跨服务直连数据库**：跨服务只走内部 HTTP 接口 `/internal/*`，携带**共享密钥 + 时间戳签名**鉴权。
3. **注册链路**：商家 App → saas-service `POST /auth/register`（校验手机号唯一）→ 调 platform-service `POST /internal/activation/claim {code, shopName, phone}`（校验激活码、建店铺、绑定、返回 `{success, shopId}`）→ saas-service 用 shopId 建老板账号 + 初始配置。
4. **激活码状态机**：`unused → used`（注册绑定）；`unused → void`（作废）；`used / void` 不可再变更。

**多租户隔离**：所有业务表带 `shopId`；后端从 JWT 解析 `userId / shopId / role`，查询强制按租户过滤；平台端接口仅运营身份访问；`/internal/*` 仅内网签名访问。

**端口速查：**

| 服务 | 端口 | 说明 |
|---|---|---|
| platform-service（后端） | **3100** | 激活码、店铺主数据、运营账号 |
| saas-service（后端） | **3200** | 商家全量业务 + 报表；收银 App 通过 10.0.2.2:3200 或局域网 IP 访问 |
| merchant-web（Vite dev） | **5175** | 商家后台 Web，proxy → 3200 |
| platform-admin-web（Vite dev） | **5173** | 平台后台 Web，proxy → 3100 |
| 收银 App（MuMu 模拟器） | 127.0.0.1:16384（adb） | MuMu 64-bit；通过 10.0.2.2 回环访问宿主机 3200 端口 |
| 收银 App（真机） | USB / WiFi adb | 通过宿主机局域网 IP（如 192.168.1.10:3200）访问 saas-service |

---

## 3. 仓库目录总览

```
d:\c\saas\
├── 餐饮收银SaaS-PRD.md             # 唯一权威需求文档（v0.3）
├── CODE-WIKI.md                    # 历史基线（部分过时）
├── 开发人员接手指南.md              # 本文档
├── 收银App开发接手指南.md           # ★ 收银 App（uni-app x）专项指南
├── merchant-web/                   # ★ 商家后台 Web（React 18 + Vite，主战场）
├── cashier-app/                    # ★ 收银 App（uni-app x / UTS 原生渲染，完整业务闭环）
│   ├── manifest.json               # appid=__UNI__539B9A9，包名 com.saas.cashier，蓝牙/WiFi/网络权限
│   ├── pages.json                  # 11 个页面路由（全部自定义导航栏 navigationStyle:custom）
│   ├── main.uts                    # createSSRApp 入口（Vue3 SSR）
│   ├── App.uvue                    # 根组件
│   ├── uni.scss                    # 样式变量（主色 #FF6A00）
│   ├── api/                        # 9 个接口模块（auth/areas/tables/dishes/orders/
│   │                               #         payments/reports/printers）
│   ├── pages/                      # 11 个页面（login/register/home/tables/order/cart/
│   │                               #       order-detail/settle/orders/print）
│   ├── utils/                      # request（Bearer token + {code,message,data}）、
│   │                               # config（BASE_URL）、esc-pos（ESC/POS 指令生成）、
│   │                               # print-manager（打印管理器，60s 缓存默认打印机）
│   └── uni_modules/saas-printer/   # 自研 UTS 原生打印插件
│       └── utssdk/app-android/     # 蓝牙 SPP（BluetoothSocket）+ WiFi TCP（java.net.Socket）
├── output/
│   ├── saas-service/               # ★ 商家端服务（NestJS，saas.db）
│   ├── platform-service/           # ★ 平台端服务（NestJS，platform.db）
│   └── platform-admin-web/         # 平台后台 Web（Vue3，有基础实现）
├── platform-service-GO-LIVE-REVIEW.md   # 平台端上线评审
├── SECURITY-AUDIT-platform-service.md   # 平台端安全审计
├── data-table-template.md          # 数据表格统一模板
├── frontend/                       # 静态原型页（index.html / merchant-login.html，非产品源码）
├── scripts/                        # deploy/ 部署脚本、deploy_remote.py
├── logo/                           # LOGO 源文件 / favicon / 说明
├── start-merchant.ps1              # Windows 一键重启：platform+saas+merchant-web
├── restart-saas.ps1                # 只重启 saas-service（build + 起服务）
├── rebuild-restart.ps1             # 双后端 rebuild + restart + 健康检查
├── copy_ref.ps1 / parse_xlsx.py    # 历史辅助脚本（可忽略）
├── seed_area.js / revert_area.js   # 桌台区域 seed/回滚 辅助脚本
└── login.yaml                      # 登录页设计稿标注（设计输入，非代码）
```

---

## 4. 快速开始（15 分钟内跑起来）

### 4.1 环境要求

| 依赖 | 版本要求 | 说明 |
|---|---|---|
| Node.js | ≥ 18（建议 20.x） | 未提供 `.nvmrc`，以本机稳定版为准 |
| npm | 随 Node | 各工程独立安装依赖 |
| Windows | — | 本仓库以 Windows + PowerShell 开发为主 |

### 4.2 首次安装

```powershell
# 1) 商家端服务
cd d:\c\saas\output\saas-service
npm install

# 2) 平台端服务
cd d:\c\saas\output\platform-service
npm install

# 3) 商家后台 Web
cd d:\c\saas\merchant-web
npm install
```

### 4.3 环境变量

两个后端各自 `npm install` 后，从 `.env.example` 复制为 `.env`（开发环境可先用示例默认值）：

```powershell
cd d:\c\saas\output\platform-service
Copy-Item .env.example .env
cd ..\saas-service
Copy-Item .env.example .env
```

关键变量（开发默认即可，**生产必须改**）：

- **platform-service**：`PORT=3100`、`DB_PATH=data/platform.db`、`DB_SYNC=false`（生产走 migration）、`SEED_OPERATOR_USERNAME=admin`、`SEED_OPERATOR_PASSWORD`（**留空则启动时自动生成随机密码并打印一次日志**）、`JWT_SECRET`、`INTERNAL_SHARED_SECRET`、`SAAS_INTERNAL_SECRET`、`CORS_ORIGINS`。
- **saas-service**：`PORT=3200`、`DB_PATH=data/saas.db`、`JWT_SECRET=dev-jwt-secret-change-me`、`PLATFORM_INTERNAL_BASE_URL=http://127.0.0.1:3100`、`PLATFORM_INTERNAL_SECRET`（**必须与 platform-service 的 `INTERNAL_SHARED_SECRET` 一致**，不一致时注册报「签名无效」——这是注册链路第一大坑）、`DEFAULT_TABLE_COUNT=10`。

### 4.4 启动（开发模式）

```powershell
# 终端 1：平台端服务（3100）
cd d:\c\saas\output\platform-service
npm run start:dev

# 终端 2：商家端服务（3200）
cd d:\c\saas\output\saas-service
npm run start:dev

# 终端 3：商家后台 Web（5175，自动打开浏览器）
cd d:\c\saas\merchant-web
npm run dev
```

> 说明：saas-service `synchronize: true` 自动建表；platform-service 受 `DB_SYNC` 控制（生产建议 false + migration）。

### 4.5 一键脚本（Windows，编译产物模式）

- `start-merchant.ps1`：杀掉 3100 / 3200 / 5175 占用进程 → 分别以 `dist/main.js`（需先 build）与 `npx vite` 重启 → curl 健康检查。
- `rebuild-restart.ps1`：`npm run build` 两个后端 → 重启 3100 / 3200 → 健康检查。
- `restart-saas.ps1`：只重建并重启 saas-service。

> 先 `npm run build` 双后端，再执行上述脚本即可用"编译产物 + 隐藏窗口"方式跑起来（适合日常联调）。
> 注意：PowerShell 里命令行直接传含 `$` 的变量会被外层转义破坏，复杂命令写成 `.ps1` 后 `-File` 执行。

### 4.6 演示账号与数据

- **平台端运营**：`admin` / 密码来源 = `.env` 的 `SEED_OPERATOR_PASSWORD`（未配置则启动日志打印随机密码，仅一次）。
- **商家端**：无预置演示账号，走**注册新店铺**流程 —— 需要一枚**激活码**。
  1. 平台后台 `output/platform-admin-web`（Vue，5173）登录运营账号 → 「激活码管理」生成一枚激活码（或直接插库）；
  2. 商家后台 `merchant-web`（5175）登录页点「注册新店铺」→ 填激活码 + 店铺名 + 手机号 + 密码 → 注册成功后即为**老板**账号。
- **种子演示账号（saas-service/scripts/seed-real.mjs 执行后）**：
  - 老板：13800000001 / pass123456
  - 收银员：13800000002 / pass123456
  - 财务：13800000003 / pass123456
  - 演示店：川香居餐厅（演示店），地址「演示路 88 号」，18 菜品 / 19 桌台 / 5 天 46 单历史数据
  - > 运行 seed 脚本前需停 saas-service（占用 3200），脚本自身会启动服务并重建数据。
- **本地数据文件**：`output/platform-service/data/platform.db`、`output/saas-service/data/saas.db`（均已 gitignore，不入库；删掉可重置环境）。

### 4.7 收银 App 环境与启动（uni-app x）

收银 App 不走 npm，工具链完全独立（HBuilderX + Android SDK + Gradle）。完整步骤见《收银App开发接手指南.md》，这里给最短路径：

| 依赖 | 版本要求 | 说明 |
|---|---|---|
| HBuilderX | **≥ 5.x（uni-app x 必需）** | 安装后必须登录 DCloud 账号（手机号绑定），否则无法获取 AppID、无法本地编译 |
| JDK | **17 LTS**（Microsoft Build 或 Oracle OpenJDK） | 本机已验证 17.0.20.1 可用；HBuilderX settings.json 写入 `uts-development-android.javaHome` |
| Android SDK | compileSdk **34**，build-tools **34.0.0**，platform-tools 最新 | 通过 sdkmanager 安装；HBuilderX 写入 `uts-development-android.sdkDir` |
| Gradle | **8.11.1**（与 HBuilderX 5.x uni-app x 兼容） | 本地安装后写入 `uts-development-android.gradleHome`；首次运行会自动下载依赖到 `~/.gradle/caches` |
| MuMu 模拟器 或 Android 真机 | — | MuMu 默认 adb 端口 **127.0.0.1:16384**（兼容 127.0.0.1:7555）；真机需开启 USB 调试 |

**首次启动（5 步）**：

```powershell
# 1) 确认后端（saas-service 3200）已启动
# 2) 启动 MuMu → 确认 adb 连接
D:\dev-tools\platform-tools\adb.exe devices
# 期望输出：127.0.0.1:16384  device

# 3) 打开 HBuilderX → 文件 → 打开目录 → 选择 d:\c\saas\cashier-app
#    菜单：运行 → 运行到手机或模拟器 → 运行到 Android App 基座
#    弹出框勾：☑ 使用自定义基座运行  ☑ 本地基座  ☑ 清理构建缓存
#    设备选 MuMu (127.0.0.1:16384)

# 4) 首次编译 3-10 分钟（Gradle 下载依赖 + UTS→Kotlin 转译 + 打包 apk）
#    成功后自动安装并启动到 MuMu，前台焦点 = UniAppActivity

# 5) App 内使用 13800000001 / pass123456 登录（需先执行 seed-real.mjs 造数）
#    BASE_URL = http://10.0.2.2:3200（模拟器→宿主机标准映射）
```

**真机联调**：改 [cashier-app/utils/config.uts](file:///d:/c/saas/cashier-app/utils/config.uts) 的 `BASE_URL` 为宿主机局域网 IP（如 `http://192.168.1.10:3200`），再重新运行/打包。

**云打包（发布 APK）**：HBuilderX 发行 → 原生 App-云打包（需 DCloud 额度；开发期推荐本地编译不排队）。

---

## 5. 工程详解

### 5.1 merchant-web（商家后台 Web，主战场）

**技术栈**：React 18 + TypeScript 5.6 + Vite 5 + dayjs + xlsx（菜品批量导入）+ antd（少量，多数为自研组件）。样式为 **CSS 变量 + 自研组件体系**，无 Tailwind。

**导航系统（务必先理解）**：

- 路由表唯一定义在 `src/data/navigation.ts`：`NAV_GROUPS`（两大分组 `ops` 运营中心 / `rpt` 报表中心）→ 菜单项 → `ViewKey`（如 `ops:dish:library`、`rpt:biz-stats`）。
- 路由是 **hash 路由**：`#/ops/dish/library` ↔ ViewKey（`App.tsx` 的 `viewKeyToHash` / `hashToViewKey`），支持手动改 URL / 前进后退 / 直达深链。
- **多页签系统**：`TabsBar` 展示已打开子页，页签状态持久化到 localStorage（`merchant_current_view` / `_group` / `_page` / `_tabs`），刷新后原样恢复。
- **星标收藏**：每个子页面右上角五角星（`FavStar` 组件包一层 `FavPage`），点亮后加入对应首页的「快捷入口」。收藏数据在 `utils/favorites.ts`，首页（`OpsHome` / `ReportHome`）的快捷入口面板动态读取。
- **主题**：`theme.ts` 黑白主题切换，CSS 变量驱动。

**目录：**

```
src/
├── App.tsx                  # 主框架（登录分流/渲染/页签/hash/收藏/主题）
├── data/navigation.ts       # 菜单 + ViewKey 路由表
├── api/                     # HTTP 封装 + 各业务 API
│   ├── http.ts              # fetch 封装：baseURL 走 vite proxy、token、统一 {code,message,data}
│   ├── types.ts             # 后端返回类型（MerchantUser 等）
│   ├── auth.ts / dishes.ts / categories.ts / attributes.ts / setmeals.ts
│   ├── buckets.ts           # 配置桶整包读写（print/stall/voucher/discount/...）
│   ├── tables.ts / areas.ts / payments.ts / orders.ts / reports.ts
├── components/              # 29 个自研组件
│   ├── TopBar / Drawer / TabsBar / FavStar / Icon / EmptyState / Pagination / Modal
│   ├── BatchImportDishModal / SelectDishesModal / DishPickerModal
│   ├── SortDishModal / SortAreaModal          # 排序弹窗
│   └── report/                                # TrendChart 等报表图表组件
├── views/                   # 38 个页面（含 reports/ 子目录 12 个）
│   ├── AuthScreen.tsx       # 登录/注册/忘记密码
│   ├── OpsHome.tsx          # 运营中心首页（概览 + 快捷入口）
│   ├── ReportHome.tsx       # 报表中心首页（趋势 + 收入构成 + 快捷入口）
│   ├── TableManage / AreaManage  # 桌台管理 / 区域管理（已拆分为两个子页面）
│   ├── CheckoutManage / VoucherManage / DiscountManage
│   ├── PrintAssign / StallManage / MustDish / BusinessMode
│   ├── PrintStyle / PrintSettings   # 票据样式 / 打印业务设置
│   ├── DeviceMonitor / OperationLog # 设备监控 / 运行日志
│   ├── DishLibrary / DishCategory / DishAttribute（属性含 spec/method/unit/topping/box/tag 六类）
│   ├── StoreProfile / StaffManage / RoleManage
│   ├── BizStats.tsx         # 综合营业统计
│   └── reports/             # PromoStats / AreaTableStats / CompareReport /
│                            # DishSalesStats / DishDiscountStats / DishDetail /
│                            # DishRefundStats / InStoreOrders / SensitiveStats /
│                            # SensitiveDetail / IncomeDiscountStats /
│                            # IncomeCouponStats / IncomeDiscountDetail
├── styles/global.css        # 全部样式与设计变量
├── theme.ts
└── utils/                   # favorites.ts / excel.ts
```

**数据架构（2026-08 已从 localStorage 全量迁移到后端）**：

| 数据类别 | 存储位置 | 说明 |
|---|---|---|
| 菜品 / 分类 / 属性 / 套餐 | 后端实体表（`/admin/dishes`、`/admin/categories`、`/admin/attributes?kind=spec\|method\|unit`、`/admin/setmeals`） | id 后端自增，页面内 `String(id)` |
| 11 个配置类模块 + 结账方式 | `shop_buckets` 桶表整包 JSON（keys：`print`/`stall`/`voucher`/`discount`/`mustdish`/`role`/`staff`/`store_profile`/`business_settings`/`payments`/`print-style`/`print-business`） | 加载 `getBucket` 有数据用之否则本地默认；保存 `putBucket` 整包 |
| UI 偏好 | localStorage | 仅 theme、tab、列设置、视图布局、认证（token/user/shop） |

**前端表格规范（所有数据页统一模板）**：模板在根目录 `data-table-template.md`，参考实现 `merchant-web/src/views/DishLibrary.tsx`。要点：`.data-table.table-list > .area-table-scroll.checkout-scroll > table.checkout-real-table`；表头 sticky + 操作列冻结（th-sticky / td-sticky）；状态用 `.status-tag.status-on/off`；空态用 `EmptyState` 且 `colSpan=总列数`；分页用统一 `Pagination` 组件（默认 10 条/页，可选 10/20/30/50）。弹窗统一 `.modal-mask > .modal-card`。

**报表中心（rpt 分组）**：14 个页面，数据来自 saas-service `reports` 模块（本地 `orders` / `order_refunds` 表聚合），支持日期范围/结账方式/门店维度筛选，金额单位「元」。

### 5.2 saas-service（商家端服务）

**技术栈**：NestJS + TypeORM + better-sqlite3（**同步 API**）+ @nestjs/jwt + bcryptjs。

**模块（15）**：auth、staff、dishes、categories、attributes、setmeals、buckets、tables、areas、payments、orders、reports、internal、devices、printers。

**实体（16）**：User、Table、Area、PaymentMethod、Dish、Order、OrderRefund、Shop、Category、Attribute、Setmeal、ShopBucket、OperationLog、Device、Printer。

**要点**：

- 统一响应 `{ code, message, data }`；错误用业务错误码。
- JWT 携带 `userId / shopId / role`；角色：老板（全量）/ 收银员（仅收银）/ 财务（仅看账，写接口拒绝）。
- 多租户：业务表带 `shopId`，查询强制过滤；A 店 token 访问 B 店数据返回 403 / 空（**测试必须覆盖**）。
- 全局守卫 `ShopStatusGuard`：店铺停用（disabled）全局拦截返回 403。
- 敏感操作（作废激活码、改价、删桌台等）写 `OperationLog`（平台端为 `OpLog`）。
- **金额约定**：全链路统一以「元」存储与传参（菜品 / 套餐 / 规格加价 / 订单 / 退款 / 报表），字段类型为 real，无分/元换算。
- 套餐分组结构：后端 DTO 无 `required/default/amount` 字段且 whitelist 会剥离，提交时不要传这些字段。
- `synchronize: true`（开发自动建表），开发期方便；生产建议 migration。
- 核心大表已建 `shop_id` 复合索引（`orders(shop_id,settled_at,status)` / `dishes(shop_id,sort_order,id)` / `order_refunds(shop_id,refunded_at)` / `operation_logs(shop_id,created_at)`）；SQLite 启动时开启 `journal_mode=WAL`（见 `main.ts`）。

### 5.3 platform-service（平台端服务）

**技术栈**：同 saas-service（NestJS + TypeORM + better-sqlite3 + JWT + bcrypt12）。

**模块（7）**：auth、codes（激活码）、shops（店铺主数据）、internal、op-log、saas-client、settings。

**实体（5）**：Operator、ActivationCode、Shop、OpLog、SystemSetting。

**要点**：

- **激活码状态机**：`unused → used`（注册绑定）/ `unused → void`（作废）；`used / void` 不可再变更。
- **种子运营账号**：启动时按 `SEED_OPERATOR_USERNAME` 自动创建 admin（bcrypt cost 12）；密码来自 `SEED_OPERATOR_PASSWORD`，未配置则随机生成并**仅在启动日志打印一次**。
- **内部接口签名**：`/internal/*` 用共享密钥 + 时间戳签名（`INTERNAL_SHARED_SECRET`），与 saas-service 的 `PLATFORM_INTERNAL_SECRET` 对齐。
- **全局限流**：Throttler 默认 60 次/分钟，登录 / 改密在 controller 单独收紧。
- **DB_SYNC 默认 false**：schema 变更走 `npm run migration:generate` / `migration:run`。
- 安全审计与上线评审：见 `SECURITY-AUDIT-platform-service.md`、`platform-service-GO-LIVE-REVIEW.md`（已覆盖：密钥强度、bcrypt、限流、激活码幂等、操作审计等）。

### 5.4 其余工程（了解即可）

| 工程 | 状态 | 说明 |
|---|---|---|
| `output/platform-admin-web` | 有基础实现 | Vue 3 + Vite（5173，proxy→3100）。视图：Login / Codes（激活码管理）/ Shops（店铺管理）/ Settings / App 壳。运营内部使用 |
| `frontend/` | 原型 | 2 个静态 HTML（index / merchant-login），非产品代码 |
| `cashier-app/`（收银 App，uni-app x） | **完整功能落地，可编译运行** | 11 个页面闭环；详见 §5.5 与《收银App开发接手指南.md》；选型为 uni-app x（UTS 原生渲染），Tauri 方案已搁置 |

### 5.5 cashier-app（收银 App，uni-app x）

**技术栈**：uni-app x + UTS（原生渲染）+ Vue 3 + 自研 UTS 原生插件（蓝牙 SPP + TCP）。编译为 Kotlin（Android），直接调用 Android 原生 API。

**架构分层**：

```
页面 (pages/*.uvue, 11 个)
   │  调用业务函数，处理 UI 状态与交互
   ▼
API 层 (api/*.uts, 9 个模块)
   │  调 request<T>()，按契约 {code,message,data} 解析
   ▼
统一请求 (utils/request.uts)
   │  BASE_URL + Bearer token + 401 回登录
   ▼
saas-service (:3200)

打印链路 (独立于 API 层)：
  页面/结账 → print-manager.uts (60s 默认打印机缓存)
                 → esc-pos.uts (生成 ESC/POS Uint8Array 指令)
                    → saas-printer (UTS 原生插件)
                         ├─ 蓝牙 SPP: BluetoothSocket (UUID SPP 00001101-...)
                         └─ WiFi TCP: java.net.Socket (9100 端口)
```

**页面清单（11 个，全自定义导航栏）**：

| 页面路径 | 名称 | 关键功能 |
|---|---|---|
| `pages/login/index` | 登录 | 手机号+密码，token 存 localStorage（key=`token`） |
| `pages/register/index` | 注册店铺 | 激活码(12位)+店铺名+地址+姓名+手机号+密码 |
| `pages/home/index` | 收银工作台 | 自定义导航栏(店铺名/角色+头像下拉)；今日营业额(仅1项)；五入口：桌台/点餐/接单/订单/打印 |
| `pages/tables/index` | 桌台点餐 | 搜索(名称/首字母) + 已就餐tabs(实时占用桌台按开台时间升序) + 其余区域tabs + 双列卡片(默认/实际人数、金额、用餐时长) |
| `pages/order/index` | 点菜 | 分类tabs(按首次出现顺序) + 菜品列表(sort_order升序，沽清置灰禁用) + 搜索时隐藏分类栏 |
| `pages/cart/index` | 购物车 | 已选菜品列表(点击弹窗删除) + 整单备注 + 底部合计 + 【立即下单】/【下单并支付】 |
| `pages/order-detail/index` | 订单详情 | 订单概览(桌台/人数/金额/时长) + 菜品明细 + 底部按钮动态：未结账→加菜/去结账；已结账→重新结账；pending→接单/拒单 |
| `pages/settle/index` | 结账 | 应收/优惠/实收/找零 + 支付方式选择 + 【确认结账】(fire-and-forget异步打印结账单) |
| `pages/orders/index` | 订单管理 | 今日订单状态tabs(全部/待接单/进行中/已结账/挂账) + 汇总条 + 订单卡片(点击进详情) |
| `pages/print/index` | 打印管理 | 打印机列表(启用/默认/编辑/测试打印) |
| `pages/print/add` | 添加打印机 | 连接方式(蓝牙/WiFi) + 蓝牙设备选择/IP端口输入 + 宽度(58/80mm) |

**关键编码约束（UTS / uvue 特有，必须遵守）**：

1. **类型只能用 `type`**，不能用 `interface`（UTS 不支持对象字面量赋值给 interface）。全量 31 个类型定义已从 interface 改为 type。
2. **条件判断类型**：`String` 不能直接做布尔条件，必须显式写 `token.length > 0`；可空对象判空用 `body != null`（不是 `body &&`）。
3. **uvue 样式限制**：不支持 `vh/deg` 单位、元素/属性/伪类/伪元素选择器、`min-height:100%`。
4. **`uni.scss` 变量**：主色 `$uni-color-primary: #FF6A00`。
5. **金额单位**：与后端一致「元」，无分/元换算。
6. **动态权限**：Android 12+ 蓝牙需 `BLUETOOTH_CONNECT/SCAN` 运行时申请（当前 saas-printer 在权限未授予时会抛异常，页面需捕获并提示用户跳转系统设置；后续可封装动态权限请求工具）。
7. **manifest.json 无 BOM**：UOS 编译会因 UTF-8 BOM 报 `Found '{' where a key name was expected`，禁止用 PowerShell 无 BOM 选项以外的方式重写 manifest。

**打印插件（saas-printer）架构要点**：

| 层级 | 文件 | 说明 |
|---|---|---|
| 业务层 | `utils/print-manager.uts` | getDefaultPrinter (60s 缓存) / printReceipt / printTest / invalidatePrinterCache |
| 指令层 | `utils/esc-pos.uts` | buildReceipt(buildTestReceipt) → Uint8Array ESC/POS 指令流 |
| 跨平台入口 | `uni_modules/saas-printer/utssdk/index.uts` | 条件编译 APP-ANDROID；sendBytes / isBluetoothEnabled / getPairedBluetooth |
| Android 原生 | `uni_modules/saas-printer/utssdk/app-android/index.uts` | sendBluetooth(BluetoothSocket SPP, 子线程 connect + write) / sendTcp(java.net.Socket, 3s connectTimeout) / isBluetoothEnabled / getPairedDevices |

> **模拟器限制**：MuMu 没有真实蓝牙硬件，蓝牙 SPP 打印无法真实出票，只能验证异常处理流程与 UI。TCP 打印需真实打印机与模拟器同 WiFi；完整打印联调必须上真机 + 真实 ESC/POS 打印机。

**后端新增配套（为收银 App 扩展的接口）**：

- Order 实体/服务：新增 `guests` 用餐人数字段（开台/点餐写入，桌台卡片实际人数数据源）
- areas.controller：新增公开 `GET /areas`（原 `/admin/areas` 仅老板，收银员会被 403）
- orders.controller：新增 `/orders/:id/reopen`（重新结账：completed/on_account → confirmed，清空结算字段、恢复桌台占用）
- orders.controller：list-orders.dto 新增日期范围参数（收银端「今日订单」功能）
- printers.*：saas-service 新增打印机配置 CRUD 接口（收银 App 的打印管理页调用）
- devices.*：收银端设备监控（`POST /devices/heartbeat` 老板/收银员心跳上报自动登记；`GET /admin/devices` 仅老板只读列表，5 分钟无心跳判离线）
- buckets.controller：新增 `GET /admin/buckets/print-style`（老板/收银员可读，收银 App 打印结账单取样式）；buckets 白名单补 `print-style` / `print-business`

---

## 6. 关键业务与编码约定（后端规范，新增代码必须遵守）

1. **技术栈**：Node + NestJS + TypeORM + better-sqlite3 + JWT。better-sqlite3 是同步 API：`SELECT` 用同步查询，**不要用 async/await 包数据库操作**。
2. **密码必须哈希存储**（bcrypt 或 argon2），禁止明文。
3. **统一响应格式**：`{ code, message, data }`；业务错误用错误码而非滥用 HTTP 状态。
4. **命名统一小写下划线**（字段、路径、数据库列名）；DTO 用 `class-validator` 校验。
5. **业务状态用枚举管理**（TS enum / union），禁止魔法字符串散落。
6. **认证与角色**：JWT 携带 `userId / shopId / role`；角色守卫：老板全量 / 收银员仅收银 / 财务仅看账。
7. **多租户**：所有业务表带 `shopId`，查询必须带租户条件，禁止跨店访问。
8. **敏感操作记录操作日志**。
9. 模块化目录：`src/modules/<业务>/` + `src/common/`（守卫/中间件/拦截器/枚举）+ `src/config/`。

**前端约定：**

- 所有数据表格复用统一模板（§5.1）；分页默认 10 条/页；排序通过顶部「排序」弹窗（上移/下移），操作列只留编辑/删除。
- 金额展示：报表「元」；提交菜品/折扣/套餐均用「元」，无分/元换算。
- 颜色体系：表格主体 `var(--color-surface2)` #F5F1EB，表头/操作列 `var(--color-surface3)` #EDE7DF，页面背景纯白；主色 `var(--color-primary)`。不要混用其他颜色。

---

## 7. 常用命令

```powershell
# merchant-web
npm run dev          # 5175
npm run build        # tsc -b && vite build（产物 dist/，含 .gz 预压缩）

# saas-service
npm run start:dev    # 开发热更
npm run build        # tsc 编译到 dist/
npm run smoke        # 冒烟测试（脚本 scripts/smoke.mjs：注册→登录→CRUD→报表链路）

# platform-service
npm run start:dev
npm run build
npm run migration:generate / migration:run   # 生产 schema 变更
```

**冒烟测试**：`output/saas-service/scripts/smoke.mjs` 会走一遍注册链路（含 platform-service internal 调用），是验证双服务联通最快的方式。

---

## 8. Git 与代码托管

| 项 | 内容 |
|---|---|
| 远程仓库（origin） | `https://github.com/bhh3349/saas.git`（GitHub） |
| 默认分支 | `main`（唯一主干分支，本地与 origin 同步） |
| 工作流 | 直接提交 `main`；功能开发建议先拉分支再合回 |
| 提交规范 | Conventional Commits：`feat(scope):` / `fix(scope):` / `chore:` / `docs:` / `refactor:`（历史提交已按此风格，新提交保持一致） |
| 提交信息语言 | 中英文均可，历史以中文为主；Windows 控制台直接写中文提交信息可能乱码，建议用 `git commit` 编辑器或先设 `core.quotepath=false` |
| 忽略清单 | 数据库文件（`*.db`）、`node_modules/`、`dist/`、构建产物、日志、`.env` 均不入库；**密钥绝不提交** |

## 9. 线上部署记录

> 2026-09-06 已登录服务器逐项核实，以下为**实际运行状态**（与早期文档记载有出入，以本节为准）。

### 9.1 部署形态（当前线上）

| 项 | 实际状态 |
|---|---|
| 云厂商 / 系统 | 腾讯云 · Ubuntu（主机名 VM-0-6-ubuntu） |
| 服务器地址 / SSH | `140.143.201.204` · 用户 `root`（端口 22，密码登录；**强烈建议尽快改密钥登录并禁用密码**） |
| 服务器目录 | `/opt/saas-prod/`（**不是**早期文档写的 `/opt/saas/`，该目录不存在） |
| 部署内容 | **仅 platform-service（激活码服务器）+ platform-admin-web 静态文件**；saas-service **未部署云端**，仅本地开发机运行 |
| 进程管理 | pm2：仅 `platform-service` 一个进程（online），Node v26.8.1 |
| 端口 | nginx 监听 **8080**（非 80）；platform-service **3100 直接公网监听**；云端无 3200 |
| 平台后台 Web | `http://140.143.201.204:8080/`（nginx 托管 `platform-admin-web/dist`，`/auth/` `/admin/` `/api/` 反代 3100，已带 CSP 等安全响应头） |
| 激活码 API | 云端 `http://140.143.201.204:3100`（直连）或经 8080 反代；注册链路（saas-service → claim）走内网签名 |
| 数据备份 | **2026-09-06 已修复**：`/opt/saas-prod/scripts/05-backup.sh`（每日 02:00 crontab，保留 30 天），首次备份已成功；此前 crontab 指向不存在的 `/opt/saas/` 且服务器上无脚本，**从未成功备份过** |

**云端与文档/仓库的差异与风险提示（接手人必读）：**

1. **云 platform-service 版本落后**：云端 `dist/main.js` 构建于 2026-08-26，本地仓库此后的改动（含 2026-09-05/06 的 devices、buckets 等）未部署。升级步骤：本地 `npm run build` → 上传 `dist/`（或整目录）→ 服务器 `pm2 restart platform-service`。platform-service 走 migration，升级前确认 `DB_SYNC=false` 下 schema 兼容（当前云端 `platform.db` 5.2MB 为生产数据）。
2. **3100 公网直接暴露**：绕过了 nginx 的安全头与日志。建议腾讯云安全组仅放行 22 / 8080，3100 收回内网（运营后台走 8080 反代已够用）。
3. **内部签名密钥**：云端 `.env` 的 `INTERNAL_SHARED_SECRET` 与本地 saas-service 的 `PLATFORM_INTERNAL_SECRET` 各自独立。若本地注册链路要调云端 claim，必须两处一致；默认本地对本地即可。
4. 备份只覆盖 platform.db（saas.db 云端不存在，脚本自动跳过）；本地开发机数据库不在备份范围。

### 9.2 部署步骤（脚本化）

```bash
# 服务器 /opt/saas-prod/scripts/deploy 下依次执行（01/02 已执行过）：
bash 03-deploy.sh         # 装依赖 + 构建 + 生成 .env + pm2 启动
bash 04-nginx.sh          # Nginx 反代 + 静态托管（注意：脚本内监听 80，线上实际手改为 8080）
bash 05-backup.sh         # 每日备份 crontab（线上实际用 /opt/saas-prod/scripts/05-backup.sh）
```

完整说明见 `scripts/deploy/README.md`。**注意**：仓库内脚本硬编码 `/opt/saas/`，云端实际目录为 `/opt/saas-prod/`，直接重跑会写到错误路径——重跑前先统一脚本内路径。

### 9.3 生产部署要点

- `.env` 全部密钥改为 ≥32 字节随机 hex；`JWT_SECRET` / `INTERNAL_SHARED_SECRET` / `SAAS_INTERNAL_SECRET` 三把钥匙分开设置；`INTERNAL_SHARED_SECRET` 必须与 saas-service 的 `PLATFORM_INTERNAL_SECRET` 一致（03 脚本会自动生成并保证三处一致）。
- platform-service：`DB_SYNC=false` + migration；`CORS_ORIGINS` 收紧到实际域名。
- 前端产物：`merchant-web` build 出 `dist/`（含 `.gz`），配合 nginx 静态托管；proxy 改由 nginx 反代到 3200 / 3100。
- 上线前逐项核对 `platform-service-GO-LIVE-REVIEW.md`。
- 验证：`pm2 status` 进程 online；`curl` 打登录接口冒烟。

---

## 10. 常见问题排查

| 症状 | 排查方向 |
|---|---|
| 前端能开但接口 404 | 确认 saas-service(3200) 已启动；vite proxy 仅覆盖 `/auth /admin /tables /reports`，新增前缀记得加 |
| 注册提示激活码无效 | 激活码在 platform.db（`activation_codes` 表）；确认已生成且状态 unused；注意两服务 `INTERNAL_SHARED_SECRET` / `PLATFORM_INTERNAL_SECRET` 必须一致 |
| 注册提示「签名无效」 | 双服务内部签名密钥不一致：platform-service `.env` 的 `INTERNAL_SHARED_SECRET` 与 saas-service `.env` 的 `PLATFORM_INTERNAL_SECRET` 必须逐字一致，改后重启两端服务 |
| 平台端登录失败 | 种子账号只有 `admin`；密码看启动日志（未设 `SEED_OPERATOR_PASSWORD` 时随机生成仅打印一次），忘记则删库重启 |
| 数据表结构变了不生效 | saas-service 删库重启（synchronize 建新表）；platform-service 走 migration |
| 端口被占用 | `Get-NetTCPConnection -LocalPort 3100/3200/5175 -State Listen` 查占用进程 |
| PowerShell 传参异常（`$` 被吞） | 写成 `.ps1` 文件后 `-File` 执行，勿在命令行内嵌变量表达式 |
| 改了 entities 但 TypeORM 报错 | better-sqlite3 是同步驱动，检查是否误用 async 包裹 DB 操作 |
| 套餐提交字段被剥离 | 后端 DTO whitelist 会剥 `required/default/amount`，前端别传 |
| 金额显示不对 | 已统一为「元」存储与传参；确认旧数据是否按分遗留（历史库需重置或迁移） |

---

## 11. 现状与待办（接手前必须知道）

1. **收银 App（uni-app x）源码已在仓库 `cashier-app/`**，11 个页面完整闭环（登录/注册/首页/桌台/点餐/购物车/订单详情/结账/订单管理/打印管理）；自研 saas-printer UTS 原生插件（蓝牙 SPP + WiFi TCP）；已通过本地 Gradle 编译生成自定义基座 APK（14.8MB），可直接运行到 MuMu。详见 §5.5 与《收银App开发接手指南.md》。
2. `output/saas-admin-web` 已删除（废弃的早期 Vue 方案），项目二商家后台以 `merchant-web`（React）为准。
3. `CODE-WIKI.md` 写于早期（设计稿转码 + localStorage 阶段），其中"纯前端 localStorage 数据"、"19 个页面"、"antd 组件体系"等描述**已过时**，以本文档 §5 与 `data/navigation.ts` 实际为准。
4. `frontend/`、`login.yaml`、`copy_ref.ps1`、`parse_xlsx.py`、`seed_area.js`、`revert_area.js` 为历史辅助产物，非产品代码。
5. 商家端角色仅固定三种（老板 / 收银员 / 财务），不开放自定义。
6. 数据表变更时，同步更新 PRD 与本文档对应章节，保证"文档 ↔ 代码"一致。
7. **2026-08 新增占位页面**（`PrintStyle` 票据样式、`PrintSettings` 打印业务设置、`DeviceMonitor` 设备监控、`OperationLog` 运行日志）均为前端配置桶存储（`print-style` / `print-business`），无后端实体；后厨档口的"店内菜品选择"使用 `SelectDishesModal` 弹窗（`dishSelectors.inStore` 存菜品 id 数组）。
8. **档口管理精简**：订单类型仅保留堂食（无外卖/自提）；收银小票5种（客单/预结单/结账单/交班单/退单）；后厨小票9种（制作单/退菜单/催菜单/起菜单/转菜单/转台单/传菜单/整桌通知单/菜品备注单）；订单来源无外卖平台（仅收银台/扫码点餐）；DishSelector 仅 inStore 渠道。
9. **2026-09-07 菜品打印配置实体化**：`dishes` 新增 `unit / serve_mode / print_enable / print_dept / temp_price_change / manual_discount / min_amount / delta_amount / fractional`；创建、修改、删除菜品会写 `operation_logs`。「打印分配」不再用 `print` 桶保存分配结果，而是读取并保存 `dishes.print_dept`；档口列表统一读取 `stall` 配置桶。票据样式左侧展示「使用中」。业务开关已收敛为移动点餐和简版收银必需项。
10. **收银 App 未完成项（2026-09-06 大部分已落地，剩余项按优先级排列）**：
   1. ~~Android 12+ 蓝牙动态权限请求封装~~ ✅ 已完成：saas-printer 新增 `requestBluetoothPermissions` / `ensureBluetoothReady`（API 31+ 申请 BLUETOOTH_CONNECT/SCAN，低版本申请定位），发送 / 扫描前自动调用
   2. ~~接单模块完整交互~~ ✅ 已完成：订单详情页 pending 状态新增「拒单 / 接单 / 去结账」三按钮（confirmOrder / rejectOrder 已接通）；扫码点餐来源订单仍属二期
   3. ~~打印管理页：蓝牙扫描设备~~ ✅ 已完成：插件新增 `startBluetoothScan`（经典蓝牙发现 + BroadcastReceiver，约 12s），add 页「已配对 + 附近扫描」合并展示，支持未配对设备（连接时触发系统配对弹窗）；add 页不再依赖 uni.* 蓝牙 API
   4. ~~打印小票样式模板化~~ ✅ 已完成：新增 `api/receipt-style.uts` 读取商家后台「票据样式-结账单」配置桶（`GET /admin/buckets/print-style`，老板/收银员可读），esc-pos `buildReceipt` 按区块开关 / 字号 / 页眉页脚生成； buckets 服务白名单已补 `print-style` / `print-business`（此前 merchant-web 票据样式与打印业务设置保存会报「无效的配置键」）
   5. ~~退出登录时清除本地打印机缓存~~ ✅ 已完成：logout 调 `invalidatePrinterCache()`（含票据样式缓存）
   6. ~~Base URL 环境切换~~ ✅ 已完成：`utils/config.uts` 环境清单（dev 模拟器 / lan 真机 / prod 云服务器），首页头像弹出层「环境切换」，选择即持久化生效
   7. ~~打印失败重试 / 队列~~ ✅ 已完成：print-manager 串行队列（蓝牙/TCP 同一时刻单连接写入）+ 失败自动重试 2 次（间隔 1s）
   8. ~~设备监控（收银端自动上报心跳）~~ ✅ 已完成：`POST /devices/heartbeat`（老板/收银员）+ `GET /admin/devices`（仅老板）+ saas-service devices 实体 + merchant-web 设备监控页 + App 端 60s 心跳（App.uvue 生命周期管理，未登录自动跳过）；冒烟 11 用例 + 浏览器端到端已验证
   9. ~~打印小票 GBK 编码~~ ✅ 已完成（2026-09-07）：`esc-pos.uts` `encodeGbk()` 通过 `java.nio.charset.Charset` 实现真实 GBK 编码；MuMu 模拟器（192.168.1.20 桥接）+ WiFi TCP 9100 端口实测 642 字节 ESC/POS 指令流，GBK 解码中文（店名/菜品/备注）全部正确，无 `??` 占位符
   10. ~~UTS 插件 HBuilderX 编译验证~~ ✅ 已完成（2026-09-07）：HBuilderX CLI（`D:\dev-tools\HBuilderX\HBuilderX\cli.exe launch app-android`）本地编译 + MuMu 运行通过；`fetchPrinters` UTSJSONObject 类型转换修复、`sendBytes` wifi 类型路由修复（`===`→`==`）、打印机列表/测试打印全链路可用

---

## 12. 开发约定与踩坑记录（实战沉淀，2026-08 确认）

> 以下规则是多次开发迭代后确认下来的**硬约定**，新增 / 修改页面时必须遵守，与 §6 编码规范互为补充。

### 12.1 前端页面规范（已全面落地）

| 主题 | 规则 |
|---|---|
| 表格结构 | `.data-table.table-list > .area-table-scroll.checkout-scroll > table.checkout-real-table`；表头 sticky；行 hover 用 `--row-hover` |
| 操作列冻结 | 表头 `th-sticky` + 数据行 `td-sticky`（global.css 已有 `position: sticky; right: 0` + 左描边），横向滚动钉在右侧 |
| 操作按钮 | `.row-actions`（gap 16px），`a.link` / `button.action-link`，无竖线分隔；危险操作加 `.danger` |
| 操作列宽度 | `按钮数 × 44 + (按钮数-1) × 16`，如 2 个按钮 = 104px，4 个按钮 = 224px |
| 其余列 | colgroup 每列 `width: 150` 等宽均分，内容过长自适应撑开 |
| 分页 | 统一 `components/Pagination.tsx`（`DEFAULT_PAGE_SIZE=10`，选项 10/20/30/50）；筛选/删除后做页码越界保护；切换 tab / 搜索重置 page=1；弹窗列表不翻页 |
| 排序 | 页面有「排序」按钮时（打开排序弹窗上移/下移），表格操作列**只留编辑/删除**，不放上移/下移 |
| 删除 | 无"默认项不可删"限制；确认弹窗统一文案（关联菜品 > 0 提示关联失效，否则提示不可恢复） |
| 状态标签 | `.status-tag.status-on` / `.status-off`；空态用 `EmptyState` 且 `colSpan=总列数` |
| 弹窗 | `.modal-mask > .modal-card`（`.modal-head > .modal-title + .modal-close`；`.modal-foot > .tm-btn.tm-btn-default/primary`） |
| 颜色 | 表格米白 `var(--color-surface2)` #F5F1EB，表头/操作列 `var(--color-surface3)` #EDE7DF，页面背景纯白，主色 `var(--color-primary)`；**禁止混用其他颜色** |

### 12.2 后端数据契约（易踩）

- **金额单位**：全链路统一「元」（实体 real 存储 + API 传参），无分/元换算，前端直接传元、直接显示。
- **套餐分组 DTO**：后端无 `required/default/amount` 字段且 whitelist 会剥离，**提交时不要传这些字段**。
- **配置桶**：`shop_buckets` 整包 JSON，keys = `print` / `stall` / `voucher` / `discount` / `mustdish` / `role` / `staff` / `store_profile` / `business_settings` / `payments` / `print-style` / `print-business`；加载有数据用之、无则本地默认，保存整包覆盖。
- **多租户**：所有业务表带 `shopId`，查询强制过滤；测试必须覆盖" A 店 token 访问 B 店数据 → 403 / 空"。

### 12.3 2026-08 关键架构决策（背景，改前必读）

1. 商家后台数据已从 **localStorage 全量迁移到后端**：菜品/分类/属性/套餐走实体表；9 个配置模块 + 结账方式走 `shop_buckets` 桶；localStorage 仅剩 UI 偏好与认证（token/user/shop）。**不要回退到纯前端存储**。
2. 报表中心新增 `rpt` 导航分组 + 快捷入口，首页（`OpsHome` / `ReportHome`）的快捷入口从 `utils/favorites.ts` 动态读取星标收藏。
3. 导航用 ViewKey + hash 路由（`#/ops/dish/library`），新增页面 = `data/navigation.ts` 注册 + `App.tsx` 挂渲染，勿直接改路由库。

### 12.4 Windows 本地开发环境坑

- **PowerShell `$` 转义**：命令行内嵌含 `$` 的变量表达式会被外层 shell 破坏，复杂命令写成 `.ps1` 后 `-File` 执行。
- **端口占用**：3100 / 3200 / 5175 被占 → `Get-NetTCPConnection -LocalPort <port> -State Listen` 定位进程。
- **better-sqlite3 同步 API**：数据库操作不要用 async/await 包装。
- **Tauri（已搁置选型）**：编译需 `[profile.dev] debug=false` 否则 Windows 沙箱 OOM；编译用 `cargo build -j 2`；当前选型为 uni-app x，不需要。
- **HBuilderX + uni-app x 本地编译常见坑**：
  1. manifest.json 必须无 UTF-8 BOM（PowerShell Write 工具默认可能带 BOM，会报 `Found '{' where a key name was expected`）；修复：用 HBuilderX 重新保存 manifest.json
  2. `interface` → 统一 `type`（UTS 对象字面量无法赋值给 interface，编译报 UTS110111163）
  3. String 不可做条件：`if (token)` → 必须 `if (token.length > 0)`；可空对象 `if (body)` → 必须 `if (body != null)`
  4. `unpackage/cache` 编译缓存脏了 → 删除整个 unpackage 目录再重新运行（相当于 Gradle clean + HBuilderX 重置）
  5. 自定义基座含 UTS 插件时，必须勾选「使用自定义基座运行」+「本地基座」，否则运行到的是标准基座（不含 saas-printer 插件，打印功能会抛"当前平台暂不支持打印"）
  6. 云打包排队问题 → 「运行到 Android App 基座」勾选「本地基座」直接本地 Gradle 编译，不排队（推荐日常开发）

### 12.5 美团收银智能版流程对照（2026-09-06 实测美团收银智能版桌面端后的结论）

> 本项目定位是美团收银智能版的精简版。以下对照基于在美团收银智能版上实际跑通「桌台点餐 / 直接点餐 / 结账 / 退单 / 反结账」全流程的实测记录，作为后续演进的参照基线。

**美团侧实测流程（参照设计）：**

1. **桌台模式**：桌台页选空桌 → 开台（人数默认=桌容量，可加会员/提成/桌台备注）→ 点菜（连点卡片累计数量、选中后可弹数字面板直接改份数、快捷备注标签少辣/不加香菜等、下单前可删除）→ 「下单」送厨房（桌台变红"待结账"，时长开始计时）→ 随时可「加菜」→ 账单页结账。
2. **直接点餐（快餐）**：不选桌直接点菜 → 结账时强制补录「餐牌号」（可跳过）→ 现金弹窗（快捷面额 + 找零）→ 结账。
3. **结账面板**：优惠三入口（订单免单 / 订单打折【折扣必选原因，入账单明细】/ 免单）+ 抹零（抹角 / 抹元 / 任意金额）+ 支付方式网格（后台可配置、可搜索、可新增）+ **混合支付**（每种支付方式记一条支付行，各自可撤销，底部「付款完成确认结账」）+ 打印结账单默认勾选。
4. **已结订单操作**（订单页）：打印小票（补打）/ 整单退（退款方式自动跟随原支付方式、现金标记"需手动为顾客退款"、退单原因必填、可打印退菜单）/ 部分退菜 / **反结账**（原因必填 → 订单退回未结账态 → 撤销原支付行 → 重新选支付方式/改菜品 → 确认后订单盖"反"章留痕）/ 操作日志全审计。
5. **交班**：开班时间 → 交班统计已结账单（未结账不计入本班次）、跨结算日拆分、交班记录（班次号/班次/交班人/本班次收款）、未交班班次无法统计收入。

**我们系统的吸收结论：**

| 流程点 | 我们现状（2026-09-06 后） | 结论 |
|---|---|---|
| 开台（人数） | 桌台页弹窗输人数 | 已有，够用；会员/提成/儿童数不吸收（超出简版定位） |
| 连点批量加菜 | 点菜页连点卡片累计 | 已有 |
| 直接改份数 | 购物车行 −/+ 步进 | 已有；「选中菜品弹数量面板」为候选 |
| 点错删除 | 购物车行删除弹窗 | 已有 |
| **菜品备注** | 购物车行弹窗「加备注」→ 订单快照 remark → 详情/小票展示；后端 items[].remark（同菜不同备注分行） | ✅ 本轮新增 |
| 下单 / 加菜 | 下单（pending→confirmed）/ 加菜合并行 | 已有 |
| **结账找零** | 结账页数字键盘 + 快捷面额 + 找零实时计算，paid_amount 下发 | ✅ 本轮新增（参照美团现金弹窗） |
| **整单打折** | 结账页 9折/8.8折/8折 chips → discount_type=discount + 名称快照 | ✅ 本轮新增（美团打折必选原因，我们用固定名称「手动X折」，候选：原因自定义） |
| 抹零 | 结账页抹零开关（抹至整数元） | ✅ 本轮新增；美团的「抹角/任意金额」为候选 |
| **退菜/退款** | 订单详情「退菜」弹层（按行选份数 + 原因必填）→ `POST /orders/:id/refund`（后端原有，本轮接 UI） | ✅ 本轮新增；部分优惠单限制同美团 |
| **撤单/撤台** | 订单详情「撤单」按钮（confirmed → void 释放桌台）；后端 reject 扩展允许 confirmed | ✅ 本轮新增 |
| 反结账 | 订单详情「重新结账」（reopen → confirmed，可改菜重结） | 已有；美团的「反结账原因必填 + 撤原支付行交互」为候选 |
| 混合支付 | 单一支付方式 + 实收/找零 | **未吸收**（需后端订单支付行模型改造，候选二期；美团为多支付行叠加各自可撤销） |
| 补打小票 | 订单详情「打印小票」（已结）/「打印预结单」（未结） | ✅ 本轮新增 |
| 交班 | 未实现（PRD 开放问题 4） | 美团参照设计已记录上表，候选后续排期 |
| 桌台状态色 | 空闲 / 占用两态 | 美团六态（空/待下单/待结账/已预约/待清台/有预订）超出简版需要，不吸收 |

**美团 POS 端设置全景（设置 → 经营设置 · 收银基础设置，18 个子 tab 实测记录）：**

| 子 tab | 关键设置项（实测值） |
|---|---|
| 点餐形式 | 桌台点餐 / 直接点餐 / 两者（对应我们的「点餐模式：桌台/叫号」） |
| 桌台 | 卡片大小、时间展示规则（开台时长）、**结账后自动清台（默认开）**、锁台、备注快捷数字、开台时长提醒、跨营业日预订 |
| 直接点餐 | **餐牌号规则（手动输入）**、餐牌号未录入提醒（可跳过）、**流水号重置时间=每日零点、起始流水号 0001 / 最大 1000**、取单规则（只能取当前设备存单）、点餐界面风格 |
| 结账 | **自动抹零方式（仅现金实款实收）**、仅现金支持找零、结账单打印默认项、自动开钱箱（现金）、记账类结账方式流水展示 |
| 交班班/日结 | **交班开关（默认开）、交班模式（明交班）、按人交班**、未完成订单是否允许交班、交班单打印时机、手动日结、现金审核备注必填 |
| 订单 | **取餐号自定义前缀**、订单默认筛选时间、**店内退单可操作时间限制（跨营业日不允许退单）**、**反结账可操作时间限制（交班后不允许反结账）**、使用反结账开关、退单默认打印退单小票 |
| 其余 | 通用点餐 / 桌台点餐 / 沽清菜品报损 / 打印 / 预订 / 订金 / 快捷入口 / 通用设置 / 点菜宝 / 押金自助餐 / 盲扫识别及自动结账；另有「常用备注」（自定义退菜原因 / 退单原因快捷标签） |

**设置对照结论：**

- **流水号/取餐号体系已对齐，无需改动**：我们 `ticket_no` 当日从 1 递增（=美团「每日零点重置 + 起始 0001」），`order_no` 按日重置（YYYYMMDD-0001）。
- ✅ 本轮已吸收：退菜快捷原因标签（对应美团「常用备注」，错点/不新鲜/上菜慢/未按要求制作/口味问题，可覆盖输入）、叫号订单详情展示 4 位取餐号。
- 候选（未吸收，记录备查）：自动抹零方式（仅现金生效）、取餐号自定义前缀、退单/反结账时间限制（依赖营业日/交班概念）、交班设置组、起始流水号可配。

### 12.6 新增功能标准流程（checklist）
1. 需求改动先同步 `餐饮收银SaaS-PRD.md`（唯一权威），再写代码；
2. 后端：新模块按 `src/modules/<业务>/` 创建，DTO 用 class-validator，实体带 `shopId`，写操作记日志，金额统一用「元」；
3. 前端（merchant-web）：页面在 `data/navigation.ts` 注册 ViewKey → `App.tsx` 挂渲染 → 列表页复用 §12.1 表格模板 → 分页/排序/删除按 §12.1 规则；
4. 前端（收银 App）：
   - 新页面 = `pages/<name>/index.uvue` + `pages.json` 注册路由 + 类型全部用 `type`
   - 新增 API = `api/<模块>.uts` + 调用统一 `request<T>()`
   - 改动后运行 `cli lsp lint --project d:\c\saas\cashier-app --platform app-android --file <文件路径>` 检查 LSP 诊断
5. 联调：双服务 `npm run start:dev` + `merchant-web` 5175；注册链路走 smoke.mjs；收银 App 跑 MuMu（BASE_URL = http://10.0.2.2:3200）；
6. 收尾：更新 PRD（如需）、本文档对应章节、《收银App开发接手指南.md》、`CODE-WIKI.md` 标注过时项。
